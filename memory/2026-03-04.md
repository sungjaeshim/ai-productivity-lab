# 2026-03-04 인사이트

## 📊 요약
- 총 청크: 1개
- 추출된 인사이트: 1개
- 처리 비용: $0.00

## 전체 컨텍스트

        주요 주제:
        1. 청크 기반 텍스트 처리의 이점
        2. 병렬 분석으로 비용 절감
        3. 자동화 시스템 구축의 중요성
        
        결론: 대규모 텍스트 처리를 위해 청킹과 병렬 처리는 필수적입니다.
        

## 💡 인사이트 (중요도순)

### #1 (0.85) #automation
- **인사이트**: 청크 기반 처리를 통해 대규모 텍스트를 효율적으로 분석할 수 있습니다.
- **태그**: #automation #efficiency #processing
- **청크**: 0

---

## 🧭 Session Distill (2026-03-04 06:11 KST)

### Decisions
- `chunk-processor`는 **B안(즉시 패치)**로 진행하기로 결정.
- 짧은 `--text` 입력도 반드시 통과하도록 최소 1청크 fallback 정책 채택.
- `datetime.utcnow()` 경고는 운영 로그 품질 저하 요인이므로 timezone-aware로 정리.
- OpenClaw health 이슈는 즉시 원인 분석 + 재발방지 설정 적용 방향으로 진행.

### State Changes (Applied)
- 파일 수정 완료:
  - `/root/.openclaw/skills/chunk-processor/main.py`
    - 청크 0개 시 `_fallback_single_chunk()`로 1청크 강제 보장
  - `/root/.openclaw/skills/chunk-processor/chunk_splitter.py`
  - `/root/.openclaw/skills/chunk-processor/analysis_engine.py`
  - `/root/.openclaw/skills/chunk-processor/storage_manager.py`
    - `datetime.utcnow()` → `datetime.now(timezone.utc)` 교체
- 검증 완료:
  - `--text` 재실행 성공 (`status=success`, `chunks=1`, `insights=1`)
  - `--file` 재실행 성공 확인
  - `utcnow` DeprecationWarning 제거 확인
- 시스템 재발방지 설정 파일 생성됨:
  - `/root/.config/systemd/user/openclaw-gateway.service.d/20-node-memory.conf`
    - `NODE_OPTIONS=--max-old-space-size=2816`
  - `/root/.config/systemd/user/openclaw-watchdog-health.service.d/10-tuning.conf`
    - fail threshold/probe/grace/cooldown 조정
  - `/root/.config/systemd/user/openclaw-watchdog-health.timer.d/10-interval.conf`
    - health 체크 주기 15분 → 5분 override

### Root Cause / Findings
- OpenClaw health alert의 직접 원인은 gateway 프로세스 OOM.
- journal 확인 결과 `FATAL ERROR: Reached heap limit Allocation failed - JavaScript heap out of memory`로 다회 크래시/자동재시작 발생.
- 부수적으로 context overflow/timeout이 혼재되어 메모리 압박을 가중하는 패턴 관찰됨.

### Lessons
- 기능 패치 완료 후에도 운영 안정성(메모리/헬스체크 주기)까지 같이 점검해야 실제 장애 대응이 끝남.
- 짧은 입력 경로(`--text`)는 파일 경로(`--file`)와 별도 회귀 테스트가 필요.
- 경고(Deprecation)도 누적되면 운영 시그널을 흐리므로 조기 제거가 유효.

### Blockers / Risks
- 재발방지 설정은 파일 반영 확인했지만, 직전 적용 커맨드가 SIGTERM으로 중단된 이력이 있어 **재가동 직후 안정성 추적 필요**.
- 근본 원인(특정 워크로드/세션에서 메모리 급증)은 추가 관측 없이는 완전 종결 아님.

### Active Tasks / WIP
- [WIP] Gateway OOM 재발 여부 모니터링 (다음 health 사이클/로그 확인)
- [WIP] 필요 시 추가 완화:
  - 대형 세션 컨텍스트 정리/리셋
  - 모델/작업 분리(고부하 채널 분산)
  - 메모리 상한 재튜닝

### Note (10:46 KST)
- 사용자 요청으로 과거 운영 패턴(PCM + GLM 병렬 + Codex 감수)을 재확인/재구성함.
- 재구성 문서 생성: `memory/second-brain/pcm-glm-codex-loop.md`
- 핵심: `pcm`은 내장 명령이 아니라 상태 즉시점검 운영 키워드(상태/포트/watchdog 교차검증).

---

## 🧭 Session Distill (2026-03-04 07:46 KST)

### Decisions
- 와치독/크론 운영은 **중복 스케줄 제거 + 고빈도 잡 축소 + 크론 보안 정리**를 최우선으로 정리하기로 함.
- TM 구축은 기존 결론 유지: **SQLite A안(v1 3테이블)** 즉시 진행.
- 사용자 요청대로 “작성 → Codex 검수” 흐름으로 진행.

### State Changes (Applied)
- 점검/현황 확인:
  - systemd timer 확인: `openclaw-watchdog-health(5m override)`, `openclaw-watchdog-sessions(2h)`, `openclaw-guard(2m)`, `openclaw-canary-monitor(10m)`
  - watchdog state 파일 확인: `health/cron-sanitize/sessions` 모두 `status=ok`
  - cron jobs 재점검: enabled 23, disabled 14, enabled error 2(timeout)
- TM 산출물 생성:
  - `/root/.openclaw/workspace/sql/tm_v1_schema.sql`
  - `/root/.openclaw/workspace/sql/tm_v1_queries.sql`
  - `/root/.openclaw/workspace/sql/tm_v1_readme.md`
- Codex 검수 및 수정 반영 완료:
  - `tm_v1_queries.sql`를 실제 스키마 컬럼명과 정합하도록 전면 교정
  - `tm_v1_schema.sql`에 FTS5 가상테이블 + 동기화 트리거 + `idx_review_action_time` 보강
  - `tm_v1_readme.md` 적용 경로/FTS5 fallback 안내 정정
  - `/tmp/tm_v1_review.db` 기준 샘플 쿼리 실행 검증 성공

### Lessons
- 초안 SQL은 컬럼명/뷰명 불일치가 가장 흔한 실패 포인트라, **검수 단계에서 실제 sqlite 실행 검증**이 필수.
- FTS5는 테이블만 만들면 끝이 아니라 **동기화 트리거(INSERT/UPDATE/DELETE)**까지 세트로 넣어야 운영 일관성 유지.
- OpenClaw 내부 `sessions_spawn/sessions_send`가 게이트웨이 타임아웃일 때는 대체 경로(로컬 Codex CLI)로 연속성 확보 가능.

### Blockers / Risks
- `sessions_spawn`/`sessions_send` 시도에서 `gateway timeout after 10000ms` 반복 발생(서브에이전트 직접 위임 경로 불안정).
- 크론/시스템 타이머 중복 및 고빈도 잡 유지 시 메모리 압박 재발 가능성 여전.
- 시스템 crontab 내 텔레그램 토큰 평문 노출 항목은 보안 리스크(즉시 회수/교체 필요).

### Active Tasks / WIP
- [WIP] TM v1 SQL을 실제 운영 DB에 dry-run 적용 후 앱 연결(UPSERT/조회 경로 교체)
- [WIP] 실패 상태 크론 2개(timeout) 실행 시간/timeout 값 조정
- [WIP] 중복 스케줄 정리안 확정(OpenClaw cron vs system cron 단일화)
- [WIP] gateway timeout(서브에이전트 경로) 원인 추적 및 복구

### Note (10:50 KST)
- 사용자 확정: PCM 운영은 **A안**으로 고정 ("pcm" 입력 시 즉시 PCM 포맷 상태보고).
- 플랫폼 정책: 텔레그램/디스코드 모두 동일 운영 키워드로 사용 가능.
- 단, 구현은 내장 slash command가 아니라 대화 문맥 트리거(운영 규약)로 동작.

---

## 🧭 Session Distill (2026-03-04 11:25 KST)

### Decisions
- 사용자 요청에 맞춰 향후 분석/체크리스트 답변 포맷을 고도화하기로 결정:
  - 기본: `요약 → 핵심3줄 → 선택지 → 다음 액션`
  - 강화: `찬성안 + 반대의견 + 개인/시스템 영향 + 미실행 비용` 동시 제공
- 링크 분석 응답은 단순 요약이 아니라 **실행 가치(너에게 이득/시스템 이득) + 단점(트레이드오프)**를 함께 제시하는 방향으로 고정.
- 범용 스킬 초안은 Christopher Alexander / Nature of Order 관점을 반영한 **First Domino Wholeness** 구조로 확정.
- 사용자가 “xhigh” 요청 시 적용할 고급 템플릿(블라인드스팟, 즉시 실행 1개, 개발 1개 포함)을 별도 템플릿으로 운영하기로 결정.

---

## 🧭 Session Distill (2026-03-04 12:55 KST)

### Decisions
- 사용자 질의(“Today → Todoist MCP 경로”)는 문서/스크립트/실행 검증 3단으로 점검.
- 운영 안전을 위해 **실제 생성 없이 dry-run 우선**으로 검증.
- 현재 라우팅 기준은 유지: `#todo` 입력 시 `brain-todo-route.sh`가 Todoist 생성 + Discord #today 알림.

### State Changes (Applied)
- 점검 수행:
  - `docs/telegram-discord-todoist-secondbrain-notion-blueprint.md` 확인 (SoT=Todoist MCP 구조 재확인)
  - `scripts/brain-todo-route.sh` 확인 (due 길이 방어, due 에러 시 무due 1회 재시도 로직 확인)
  - `scripts/brain-todoist-sync.sh` 확인 (DOING→active+today 정책 확인)
- 환경 상태 확인:
  - `/root/.openclaw/credentials/todoist`, `todoist-projects.json` 존재 확인.
- dry-run 검증 결과:
  - `./scripts/brain-todo-route.sh --raw "#todo 테스트 태스크 | today | p1" --dry-run`
    - project: `active`, due_date: `2026-03-04`, Discord #today 알림 예정으로 정상 파싱.
  - `./scripts/brain-todoist-sync.sh --item-id "check-20260304" --status DOING --title "today->todoist sync check" --dry-run`
    - active/p1/due today 매핑 정상.

### Lessons
- 최근 실패 원인이던 Todoist `due` 150자 제한 케이스는 라우터에서 메타데이터 제거/길이 제한/재시도로 완화되어 있음.
- 경로 검증은 문서 확인만으로 부족하고, **dry-run 출력 확인**까지 해야 운영 신뢰도가 올라감.

### Blockers / Risks
- 이번 점검은 dry-run만 수행했으므로 실제 API 호출/실제 알림 전달 성공 여부는 미검증.
- 라우팅 원문에 미디어 메타가 과도하게 섞이면 여전히 `due` 파싱 노이즈 리스크가 존재.

### Active Tasks / WIP
- [WIP] 필요 시 실검증 1건 수행(생성→#today 알림→완료/정리까지 end-to-end).
- [WIP] 라우팅 실패 로그(특히 due 관련 400) 재발률 모니터링.

### State Changes (Applied)
- 사용자 전달 산출물 생성(대화 내):
  - `first-domino-wholeness` 스킬 본문(확장형)
  - `first-domino-wholeness-lite` 1페이지 압축본
  - xhigh 분석 템플릿 v2(찬반/영향/블라인드스팟/즉시실행/개발 분리)
- X 링크 요약 파이프라인 우회 확인:
  - 직접 X/쿠키 기반 수집 실패 시 `r.jina.ai` 경유로 본문 추출 가능함을 재확인.
- 메모리 조회 이슈 발생:
  - `memory_search`가 embedding/provider timeout으로 unavailable 상태였고,
  - 파일 직접 스캔(`rg`/`read`)으로 메타인지/메타데이터 관련 과거 기록을 수동 교차검증.

### Lessons
- 사용자는 체크리스트 자체보다 **왜 해야 하는지(이득/손실)와 반대 시각**을 함께 볼 때 실행 의사결정이 빨라짐.
- 동일 포맷이라도 목적이 다르면 분리 운영이 필요함:
  - 일상 브리핑 포맷
  - xhigh 심화 포맷
- 메모리 검색 장애 상황을 대비해 **로컬 파일 grep fallback**은 실무적으로 유효하나, 노이즈 파일(대용량 리포트/번들)은 검색 범위 제한이 필수.

### Blockers / Risks
- `memory_search` provider timeout 재발 시 과거 맥락 조회 신뢰도가 저하될 수 있음.
- 메모리 디렉토리에 비정형/대용량 파일이 많아 수동 검색 시 잡음이 커짐(검색 정확도/속도 저하).
- 제2의 뇌 메타인지/메타데이터 통합은 방향 합의는 됐지만, 실제 인덱스 파일 생성/연결 자동화는 아직 미적용.

### Active Tasks / WIP
- [WIP] 사용자 맞춤 응답 포맷을 “찬성/반대 + 개인/시스템 영향 + 비용”으로 지속 적용
- [WIP] xhigh 템플릿을 실제 운영 질의에 반복 적용해 품질/부담도 검증
- [WIP] second-brain 통합 인덱스(`index.md`) 설계안 작성
  - 태그 규칙
  - 인사이트↔결정↔패턴 링크 룰
  - AC2 `meta/` 참조 매핑
- [WIP] memory_search 장애 원인(embedding/provider timeout) 점검 및 재발 시 fallback 표준화

---

## 🧭 Session Distill (2026-03-04 14:11 KST)

### Decisions
- 사용자 요청으로 OpenManus 대화 내용을 "재사용 가능한 제2의 뇌 인사이트"로 즉시 저장.
- 저장 위치는 second-brain `insights/` 개별 문서 + `insights.md` 인덱스 동시 반영으로 결정.

### State Changes (Applied)
- 신규 인사이트 문서 생성:
  - `memory/second-brain/insights/2026-03-04-OpenManus-실전운영-인사이트.md`
- 인덱스 업데이트:
  - `memory/second-brain/insights.md` 상단에 2026-03-04 OpenManus 항목 추가

### Captured Insight
- OpenManus 실전 핵심은 `Planner(LLM) + Executor(도구) + Judge(피드백)` 루프이며,
  브라우저 자동화는 도구 연결/호출 조건에서만 동작.
- 로그인/2FA는 사람 개입 1회 후 세션 재사용 방식이 현실적인 운영 패턴.
- ROI는 단발 답변보다 반복형 리서치/수집/정리 자동화에서 크게 발생.

### Active Tasks / WIP
- [WIP] 필요 시 OpenManus용 "멈춤→인증→재개" 프롬프트 템플릿 및 체크리스트 별도 문서화.

---

## 🧭 Session Distill (2026-03-04 11:29 KST)

### Decisions
- 사용자 요청으로 TM v1 상태를 “완/미완”으로 재판정:  
  - **AC2 검색 보조 관점 = 완료**  
  - **전체 운영 경로 스위치 관점 = 미완**
- 사용자가 `A`를 선택하여, 운영 스위치 1단계(조회 경로)를 즉시 반영하기로 결정.
- 전환 리스크를 낮추기 위해 **TM-first 기본 + docs-first 옵션 유지** 전략 채택.

### State Changes (Applied)
- 코드 변경 완료:
  - `/root/.openclaw/workspace/memory/second-brain/ac2/agilestory/scripts/phase3_search.py`
    - 기본 라우팅을 `TM-first`로 전환
    - 플래그 추가/정리: `--tm-only`, `--no-tm`, `--docs-first`
    - 기본 동작: TM 조회 → 미히트 시 docs FTS fallback
- 동작 검증 완료:
  - 기본 실행 시 `tm_fallback.mode = "tm_first"` 확인
  - `--docs-first` 실행 시 `mode = "docs_first"` 확인
- 운영 상태 재확인:
  - TM 스키마/DB 반영 상태(테이블/인덱스/FTS 트리거) 유지 확인

### Lessons
- “완료”는 관점에 따라 다르므로, **기능 완료와 운영 경로 전환 완료를 분리 선언**해야 혼선이 줄어든다.
- 전면 전환보다 **조회 경로 선전환(읽기 먼저)**이 운영 리스크를 크게 줄인다.
- 롤백/비교 플래그(`--docs-first`)를 남겨두면 신뢰 검증과 장애 대응이 빨라진다.

### Blockers / Risks
- UPSERT(쓰기) 경로는 아직 TMStore로 완전 통일되지 않아, 엄밀히는 2단계 전환이 남아 있음.
- TM 데이터 커버리지가 낮은 구간에서는 TM-first에서 docs fallback 빈도가 높을 수 있음(성능/일관성 체감 편차).
- 운영 호출부 전체가 `phase3_search.py` 기반으로 통일된 것은 아니므로, 주변 스크립트 경로 점검이 추가로 필요.

### Active Tasks / WIP
- [WIP] 2단계 전환: UPSERT/쓰기 경로를 TMStore 기반으로 통일
- [WIP] 조회 경로 전환 후 품질 지표 점검
  - TM hit rate
  - fallback 비율
  - 결과 일관성(용어/문장)
- [WIP] 운영 호출부 전수 점검(기존 docs-only 호출 잔존 여부 확인)

---

## 🧭 Session Distill (2026-03-04 11:48 KST)

### Decisions
- NQ 알림 품질 개선 방향 확정:
  - **Discord는 풀포맷 상시 전송**(강제 출력 포함)
  - **Telegram은 이벤트성만 전송**(골든/데드/시장급변)
- 시장급변 감지는 “상태 지속”이 아닌 “이벤트” 중심으로 전환:
  - VIX는 `vix>=20` 상시 ALERT 방식 폐기
  - `경계 돌파 + 급변 + 쿨다운`으로 변경
- 감성 델타는 노이즈 감소를 위해 임계치/쿨다운 상향:
  - 기존 `|delta|>=8` → `|delta|>=12`
  - 90분 쿨다운 + 방향 이벤트(`UP/DOWN`) 적용
- 메시지 포맷은 중복 제거 원칙 확정:
  - 매크로 블록에서 수치 1회 표기
  - 하단 시장급변은 **이벤트 라벨만** 표기

### State Changes (Applied)
- 크론 잡(📈 NQ MACD 신호 감지, `4d5b8d9a-...`) 프롬프트/동작 재구성:
  - 요약형 `[NQ MACD + 시장 급변 통합]` 제거
  - 풀포맷 원문 기반 전송으로 복구
  - `--force-live` 적용으로 빈 출력 방지
  - Discord/Telegram 동시 전송 경로 복구 후 채널 분리 정책 적용
- VIX 로직 코드 변경:
  - 파일: `/root/.openclaw/workspace/scripts/vix_alert.py`
  - 이벤트 타입 도입: `VIX_CROSS_UP`, `VIX_CROSS_DOWN`, `VIX_SPIKE`
  - 90분 쿨다운 + 상태파일 내 alerts 타임스탬프 기록
- 감성 로직 코드 변경:
  - 파일: `/root/.openclaw/workspace/scripts/market_sentiment_delta.py`
  - 이벤트 타입 도입: `SENTIMENT_JUMP_UP`, `SENTIMENT_JUMP_DOWN`
  - 임계치 12/쿨다운 90분 적용
- NQ 본문 포맷 변경:
  - 파일: `/root/.openclaw/scripts/nq_macd_multi.py`
  - 시장급변 상세 수치 재표기 제거
  - `⚡ 시장급변 감지: <이벤트 라벨>` + `(세부값은 위 매크로 블록 참조)`로 통일

### Root Cause / Findings
- 텔레그램 누락 원인: 스크립트 내부 `send_telegram()` 의존 + 환경 변수(`TELEGRAM_BOT_TOKEN`) 불완전 상태.
- 텔레그램 과다 전송 원인: “시장급변 블록 존재” 조건이 감성 ALERT 단독에도 충족되어 발생.
- VIX 반복 ALERT 원인: 기존 로직이 레벨 조건(`vix>=20`)만 체크하고 이벤트 경계/쿨다운이 없었음.

### Lessons
- 알림 설계는 **상태(state)**와 **이벤트(event)**를 분리해야 소음이 줄어든다.
- 동일 값의 중복 표기는 해석 피로를 만든다(숫자는 1회, 이벤트는 라벨만).
- 채널 목적이 다르면 전송 정책도 분리해야 한다(Discord 로그형, Telegram 이벤트형).

### Blockers / Risks
- 크론 프롬프트 지시 기반 로직은 추후 편집 시 회귀 가능성 있음(조건 누락/완화).
- Telegram 이벤트 조건이 현재는 “시장급변 블록/크로스 문자열” 기반이라 문자열 변경 시 오동작 여지 존재.

### Active Tasks / WIP
- [WIP] Telegram 조건 추가 정밀화 여부 결정:
  - 감성 단독 ALERT 제외(= VIX/USDKRW ALERT 또는 크로스만) 옵션 검토
- [WIP] 2~3 사이클 모니터링으로 알림 빈도/유용성 확인
- [WIP] 필요 시 이벤트 판정 로직을 크론 프롬프트가 아닌 코드 플래그로 이관(회귀 방지)

---

## 🧭 Session Distill (2026-03-04 15:38 KST)

### Decisions
- `#today` 채널 운영 원칙을 “많은 항목 개별 처리”에서 **배치+판정(PCM)**으로 전환.
  - P(Process) / C(Check) / M(Mark) 포맷으로 5개 핵심 항목만 처리.
- Inbox→Today→Todoist 라우팅에서 프로젝트 분류 누락 문제를 우선 해결하기로 결정.
- `#todo` 입력 포맷에 프로젝트/상태 토큰을 명시적으로 허용하기로 확정:
  - project: `queue|active|waiting|inbox`
  - status hint: `TODO|DOING|DONE|BLOCKED` (한국어 별칭 포함)
- 후속 자동화는 B안 채택:
  - Discord 반응(✅/🔄/🚫)을 Todoist 상태로 동기화하는 흐름 구축.

### State Changes (Applied)
- 스캔/분류 수행:
  - `#today` 최근 메시지 전수 조회 후 `작업/보류/완료` 분류.
  - 확인 결과: 수동 실행 가치 항목보다 자동 로그성 항목 비중이 큼(노이즈 과다).
- 라우터 개선 적용:
  - 파일: `/root/.openclaw/workspace/scripts/brain-todo-route.sh`
  - 변경:
    - project/status 토큰 파싱 추가
    - due 파싱 시 project/status/priority 토큰 제외 처리
    - 파싱 결과에 `project_override`, `status_hint` 포함
  - dry-run 검증:
    - `#todo ... | waiting | p2` → waiting 프로젝트 매핑 정상
    - `#todo ... | DOING | today | p1` → active + due today 정상
    - `#todo ... | queue | p3` → queue 정상
- Todoist sync 경로 보강:
  - 파일: `/root/.openclaw/workspace/scripts/brain-todoist-sync.sh`
  - 변경:
    - `.brain-todoist-registry.jsonl` 외에 `.todo-router-registry.jsonl` fallback 조회 추가
    - `event_id` 기반으로 `todoist_id` 역참조 가능하게 확장
- 새 자동화 스크립트 추가:
  - 파일: `/root/.openclaw/workspace/scripts/brain-todo-reaction-sync.sh`
  - 기능:
    - `✅ #todo 라우팅 완료` 메시지를 읽어 `event_id` 추출
    - 반응 이모지 매핑: `✅→DONE`, `🔄→DOING`, `🚫→BLOCKED`
    - 변화 시 `brain-todoist-sync.sh` 호출하여 상태 반영
    - state 파일(`.todo-reaction-sync-state.json`)로 중복 처리 방지
  - 실행 검증:
    - dry-run 및 실실행에서 현재는 변경 반응 없음(`ACTIONS:0`) 확인

### Lessons
- 운영 채널은 “모든 이벤트 표시”보다 **실행 대상 압축 + 종료 판정 기준**이 훨씬 중요.
- 라우팅 정확도는 자연어 추론보다 **명시 토큰(project/status)**이 안정적.
- 상태 동기화는 2단계로 보는 게 안전:
  1) task 생성 라우팅 안정화
  2) reaction 기반 상태 전이 자동화

### Blockers / Risks
- `e2e-check 125620`은 라우팅 레코드만 존재하고 pass/fail 산출물 링크가 없어 완료 검증 미흡.
- “영문+한글주석 수정” 건은 실제 파일 경로 미지정으로 작업 종결 판정 불가.
- 반응 동기화 스크립트는 구현됐지만, 아직 실제 반응 이벤트(✅/🔄/🚫)가 없어 end-to-end 상태 전이 실증은 미완.
- 중간에 게이트웨이 재시작/세션 끊김이 있어 작업 연속성 관리(재시도/재개 메시지) 필요성 재확인.

### Active Tasks / WIP
- [WIP] `brain-todo-reaction-sync.sh`를 주기 실행(크론/타이머)으로 연결해 완전 자동화 마감
- [WIP] 반응 1건씩 실증 테스트(🔄→✅→🚫)로 Todoist 상태 전이 E2E 검증
- [WIP] `#today` 운영 룰 고정:
  - 자동 로그성 항목 축소
  - WIP 최대 3
  - daily cut 시 `완료/보류/블록` 강제 판정
- [WIP] 파일 경로 없는 요청은 “실행 전 필수 필드” 체크(경로/근거 링크)로 선차단


# 📋 Daily DONE Summary: 2026-03-04

No completed items for this date.

---
_Generated by brain-daily-done-summary.sh_

## 🧭 Session Distill (2026-03-04 21:42 KST)

### Decisions
- 사용자 요청 확정:
  - 웹 리서치 운영은 **Brave 메인 + DDG 폴백**으로 고정.
  - 검색/뉴스 답변에 출처 태그 `[Brave] [DDG] [RSS]` 기본 표기.
  - 저녁 회고/heartbeat 완료 보고는 Discord 브리핑 로그에도 함께 기록.
- CI 모니터 오탐(일시 502) 대응은 **B안 채택**: 502/503/504 1회 재시도 후 알림.

### State Changes (Applied)
- 사용자 선호 업데이트:
  - `USER.md`에 Discord 브리핑 동시 기록 선호 추가.
  - `USER.md`에 Brave+DDG 폴백/출처태그 선호 추가.
- 검색 운영 문서화:
  - `docs/ops/SEARCH_FALLBACK_PLAYBOOK.md` 생성/수정
  - 폴백 트리거에 `401/402/429`(토큰/쿼터) 추가, RSS 병합 정책 반영.
- 뉴스 수집 품질 개선:
  - `scripts/daily-intelligence-collector.sh` 고도화(다중 피드, dedup, 도메인 다양성 cap, 품질 메타 포함).
  - 실행 검증 성공, 산출물 갱신: `data/intelligence-2026-03-04.json`.
- GitHub CI 모니터 노이즈 완화 패치:
  - `scripts/monitor-brain-link-ci.sh`
  - `scripts/github-ci-monitor.sh`
  - 502/503/504 시 2초 대기 후 1회 재시도 로직 반영 + bash 문법 검증 통과.

### Findings / Root Cause
- `brain-link-ingest-test` 알림의 핵심 원인은 CI 실패가 아니라 **GitHub API 일시 502**.
- “중간에 한 단어만 따로 전송” 현상은 Telegram `streaming: partial` 환경에서 청크 분할로 발생 가능.
- `edit failed` 알림 원인은 파일 편집 대상 문자열 중복으로 인한 모호 매칭(이후 문맥 확장으로 재적용 성공).

### Lessons
- 운영 자동화는 기능 적용보다 **오탐/노이즈 제어(재시도·중복억제)**가 체감 가치가 큼.
- RSS 품질은 도메인 다양성 관리/중복 제거를 넣어야 실제 브리핑 품질이 오른다.
- 설정 스키마에 없는 기능(DDG provider)은 엔진 레벨이 아닌 **운영 레벨 폴백**으로 명시 관리해야 혼선이 줄어든다.

### Blockers / Risks
- OpenClaw 설정 스키마상 `tools.web.search`에 DDG provider/fallback 필드가 없어 완전 자동 엔진 폴백은 미구현.
- DDG 쿼리는 소스 편향/결과 흔들림 가능성이 있어 고신뢰 이슈는 교차검증 필요.
- Telegram partial streaming 유지 시 단어 단위 분리 전송 재발 가능.

### Active Tasks / WIP
- [WIP] Brave 실패/쿼터소진 시 DDG 재조회 + RSS 병합을 실브리핑에서 지속 적용.
- [WIP] 검색 답변 출처 태그(`[Brave]/[DDG]/[RSS]`) 누락 없이 운영.
- [WIP] 필요 시 Telegram 전송 모드(부분 스트리밍) 완화 여부 결정.
- [WIP] Vercel/Supabase/Sentry 무료티어 최적화안 최종 확정(특히 SERVICE_ROLE 노출 리스크 점검).

