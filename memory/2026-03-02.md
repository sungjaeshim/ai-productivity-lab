# 2026-03-02

## Preferences
- Ops 시작 트리거 규칙 확정:
  - 사용자가 "여기(텔레그램)" 또는 "디코"에서 "추가로 작업하자/시작하자"라고 명시하면 ops 시작으로 간주.
- 네트워크/Telegram 계열 장애 대응 선호:
  - 사용자는 장기간 사용 경험상 IPv6 관련 장애 빈도가 높다고 판단.
  - 향후 유사 장애(특히 fetch failed/ETIMEDOUT/ENETUNREACH, Telegram media download 실패) 발생 시 **IPv4 우선 가설을 1순위**로 점검/적용하기를 요청.
- 링크/글 분석 응답 선호 업데이트:
  - 사용자가 링크나 글을 붙여주면 추가 질문 없이 즉시 분석 수행.
  - 기본 출력은 `핵심구조 → 인사이트 → 우리 적용안(실행 우선순위)` 형태로 제공.
  - 가능하면 바로 적용 가능한 체크리스트/다음 액션까지 포함.
- 모닝 브리핑 포맷 선호 업데이트:
  - 카드형/장식형보다 간결한 섹션형(한 줄 요약 + TOP3/리스크/결정/핵심액션)을 선호.
  - 가독성 우선(짧은 문장, 중복 최소화).

## Decisions
- AGENTS.md에 Automation-First Prompt Policy(영문 + 한글 주석) 반영.
- 제품 운영 의사결정 템플릿(RFE→RFT→FUT)을 AGENTS.md에 추가.
- 재사용 스니펫 분리를 위해 PROMPTS.md 신설.
- X 링크 분석 기본 방침: x/fx/vx/fixvx 순으로 자동 재시도 후 실패 시 Relay 안내.
- macOS에서 Relay 설정 시 서버 경로 안내는 잘못이며, 로컬 경로/로컬 CLI 필요 조건을 명확히 분리하기로 함.

## State Changes
- Gateway config 변경 적용:
  - Discord `groupPolicy`를 `allowlist`로 변경.
  - `plugins.entries.query-router` stale 항목 제거.
  - Telegram 네트워크 안정화: `channels.telegram.network.autoSelectFamily=false`, `dnsResultOrder=ipv4first` 적용 및 게이트웨이 재시작.
- 설정 patch 적용 후 게이트웨이 재시작 정상 완료.
- 현재 설정 점검상 query-router stale 경고 해소됨.
- Telegram 이미지 수신 재테스트: 수정 후 정상 수신 확인.

## Lessons Learned
- 동일 안내 문구 중복 전송 실수 발생 → 단일 응답 원칙 재확인.
- X 본문 접근은 현재 환경에서 다수 UI_LOCK 발생(직접 x.com, fxtwitter, vxtwitter, fixvx 모두 실패 케이스 다수).
- Browser Relay 설정에서 `!` 배지는 대체로 relay 인증/연결 문제.
- 사용자 PC(맥북) 작업은 서버 경로(`/root/...`)가 아닌 로컬 경로 기준으로 안내해야 함.
- `Failed to download media`의 실원인 로그 확인:
  - Telegram media fetch 실패 + sendChatAction 네트워크 실패 동시 발생
  - 파일 자체보다 Telegram API 연결(일시 네트워크) 이슈 가능성 큼.

## Blockers
- 사용자 측 Chrome Relay 탭 attach 미완료(브라우저 tabs 0개 상태 지속).
- macOS 환경에 OpenClaw CLI 미설치(`openclaw: command not found`)로 로컬 확장 설치 절차 진행 불가.
- X 링크 자동 추출은 현재 UI_LOCK로 실질 차단 상태.

## Active Tasks / WIP
- 사용자 대기 상태:
  1) Relay를 실제로 쓸지(맥북 CLI 설치 포함) 결정
  2) 또는 설치 없이 텍스트/스크린샷 기반 분석 지속
- Relay 경로 선택 시 후속 할 일:
  - macOS에 OpenClaw CLI 설치
  - `openclaw browser extension install/path` 실행
  - Chrome Load unpacked + 배지 ON 확인
  - 이후 대기 중 X 링크 일괄 재분석

## Cron Monitoring Notes
- `brain-link-ingest-test` CI 모니터는 여러 차례 실행되었고 모두 중복 실패(DUP) 또는 신규 없음으로 알림 미전송.

## Morning Briefing (08:00 KST)
- 통합 모닝 브리핑 전송 완료 (Telegram target 62403941, messageId: 23530)
- 5카드 구조: 시장/시그널, 일정/프로젝트, 리스크, 의사결정, 핵심 액션
- 핵심 액션: Cron 오류 3건 복구 (저녁회고/Ralph Loop/장마감)

## Preferences (09:15 추가)
- 세션 컨텍스트/씽킹 운영 기준 고정:
  - 컨텍스트 0~50%: 정상 진행(최적 구간)
  - 50~65%: 장문 작업 축소, 큰 작업 시작 전 `/new` 고려
  - 65%+: 새 세션 권장(리셋/지연 체감 급증 구간)
- 씽킹 사용 규칙:
  - 메인 세션은 `medium` 기본 고정
  - `high`는 필요 시에만
  - `xhigh`는 "짧고 비싼 의사결정"에만 1회성 스팟 사용(상시 사용 금지)

## Decisions (12:38 추가)
- NQ MACD cron(Job ID: `4d5b8d9a-9c43-4b7b-bea9-d80524b7804d`) 복구 전략 확정:
  - 실행 주체를 `group-sonnet`에서 `main`으로 전환
  - 모델을 `openai-codex/gpt-5.3-codex`로 고정
  - 누락된 `env-loader.sh` 의존 제거, `python3 /root/.openclaw/scripts/nq_macd_multi.py` 직접 실행
- 사용자 상태체크 키워드 `pcm`은 "현재 작동 상태 즉시 확인"으로 운영.
- Second Brain 이슈는 "서비스 다운"이 아니라 "데이터 소스 공백/경로 불일치"로 분류.

## State Changes (12:38 추가)
- NQ MACD cron 강제 실행 결과 `HEARTBEAT_OK` 확인, `consecutiveErrors` 0으로 리셋.
- cron payload 규칙 업데이트 반영:
  - 정상 시 `HEARTBEAT_OK` 1줄
  - 에러 시 3줄 요약(재시도 금지)
- 외부 링크 점검:
  - Karpathy microgpt URL 정상 응답(HTTP 200)
  - growth second-brain URL/API 정상 응답(HTTP 200)
- Second Brain 저장소 상태 확인:
  - `/root/.openclaw/workspace/memory/second-brain`에 핵심 파일 다수 누락(`insights.md`, `decisions.md`, `patterns.md`, `curated-reads.md`, `investment/...`, `ac2`)
  - `/root/Projects/growth-center/data/second-brain-data.json`에는 과거 데이터(예: insights 53건) 존재

## Lessons Learned (12:38 추가)
- "화면에 아무것도 안 나옴"은 프론트 장애가 아니라 백엔드 소스 파일 공백일 수 있으므로, API 상태(200)와 데이터 길이([])를 분리 점검해야 함.
- cron 실패 원인 분리 순서가 효과적이었음: (1) 모델/인증 (2) 실행 경로/스크립트 의존성.
- `runs` 히스토리의 최신 성공 요약(`HEARTBEAT_OK`)과 `consecutiveErrors`를 함께 봐야 실제 복구 여부를 빠르게 판단 가능.

## Blockers (12:38 추가)
- Second Brain 기존 데이터가 마크다운 소스에서 비어 있음(경로 변경/초기화/미동기화 원인 미확정).
- 브라우저 도구 host profile은 포트 충돌(`18800 in use`)로 직접 시각 검증 제한(HTTP/API로 우회 검증 수행).

## Active Tasks / WIP (12:38 추가)
- 진행 중:
  1) Second Brain 데이터 소실 원인 추적(저장 경로 변경 vs 초기화)
  2) `second-brain-data.json` 기반 마크다운 복원 여부 결정 대기(사용자 승인 필요)
- 후속 예정:
  - 복원 수행 시 백업 후 복구 → `/api/second-brain/dashboard` 재검증 → UI 재확인
  - 아침에 남은 실패 cron(시장 급변 감지/저녁 회고/Ralph Loop) 동일 패턴으로 추가 복구

## State Changes (12:47 추가)
- 사용자 요청("적용하게 해줘")에 따라 Agentic Engineering 실행 프레임을 운영 문서에 반영.
- `/root/.openclaw/workspace/PROMPTS.md`에 아래 항목 추가:
  - `Agentic Engineering 실행 체크리스트 (10줄)`
  - `실행 명령형 프롬프트 (복붙용)`

## Decisions (15:06 추가)
- OpenClaw v2026.3.1 업데이트는 완료로 확정.
- 신규 기능 적용 우선순위는 "기능 확대"보다 "운영 안정화"로 정리.
- 저녁 회고 크론(21:00) `network_error` 재발 방지를 위해 실행 모델을 GLM-5에서 Codex로 전환.

## State Changes (15:06 추가)
- 게이트웨이/서비스 상태는 정상(`openclaw status` 기준 running, port 18789 listening).
- 저녁 회고 크론(Job ID: `1f81c366-2df7-4d4d-a866-8e500674ce09`) 업데이트 완료:
  - `payload.model`: `zai/glm-5` → `openai-codex/gpt-5.3-codex`
- Second Brain API 상태 재점검:
  - dashboard는 카테고리 카운트 정상(insights/decisions/patterns/ideas/investment/ac2)
  - captures는 `total: 0` 상태 지속

## Lessons Learned (15:06 추가)
- `Unhandled stop reason: network_error`는 포트 다운이 아니라 모델 호출 경로 실패일 수 있음.
- 크론 안정화는 "서비스 헬스"와 "모델 경로 안정성"을 분리해 점검해야 빠르게 원인 분리 가능.

## Blockers (15:06 추가)
- captures 데이터는 조회 API/프론트가 아니라 원천 로그(`capture-log.jsonl`) 자체 부재가 1차 원인으로 추정.
- 워치독은 아직 `/health`/`/ready` 기반이 아니라 포트 리슨 체크만 수행 중(오탐 가능성 잔존).
- 보안 audit critical(소형 fallback + sandbox/web tool 제한) 미해결 상태.

## Active Tasks / WIP (15:06 추가)
1) captures 파이프라인 원천 로그 생성 지점 복구(쓰기 주체 재연결)  
2) 워치독 health/ready 체크 방식으로 전환  
3) 보안 critical 해소(sandbox mode + web tool 제한 정책 반영)  
4) 업데이트 기능 6개를 즉시적용/보류로 나눠 순차 반영

## Decisions (15:34 추가)
- 미완료 보완 항목 중 즉시 조치 2건을 우선 적용:
  1) 워치독 헬스체크를 포트 기반 단일 판정에서 `/health`/`/ready` HTTP probe 우선 방식으로 전환.
  2) 보안 critical 대응으로 기본 sandbox를 `all`로 상향하고 웹 계열 도구를 전역 deny 처리.
- 안정화 우선 원칙 유지: 신규 기능 확대보다 오탐/보안 리스크 제거를 먼저 처리.

## State Changes (15:34 추가)
- `/root/.openclaw/scripts/openclaw-watchdog-health.sh` 수정 완료:
  - 포트 리슨 확인 후 `http://127.0.0.1:<port>/health`, `/ready` 2xx 응답을 건강 판정 기준으로 사용.
  - 자동 재시작 후 복구 판정도 동일 HTTP probe 로직으로 통일.
- Gateway config patch 적용 + 재시작 완료:
  - `agents.defaults.sandbox.mode = "all"`
  - `tools.deny = ["group:web", "browser"]`
- 업데이트 버전 상태는 `2026.3.1` 유지.

## Lessons Learned (15:34 추가)
- health 감시는 "포트 열림"만으로는 충분하지 않음. 서비스 준비 상태(`ready`) 확인이 오탐 감소에 직접적.
- 보안 audit critical은 설정 패치 한 번으로 빠르게 줄일 수 있으므로, 운영 이슈 대응 루틴에 즉시 포함하는 게 효율적.

## Blockers (15:34 추가)
- captures 공백 이슈는 여전히 미해결(원천 로그 파일 부재 추정 유지).
- 6개 적용 패키지 중 나머지 항목(light context/Discord lifecycle/WS warm-up 등)은 아직 순차 적용 대기.

## Active Tasks / WIP (15:34 추가)
1) captures ingest/write 경로 복구(로그 생성 주체 재연결)  
2) 보안 audit 재실행으로 critical 해소 최종 확인  
3) 남은 업데이트 적용 항목(6개 패키지 잔여분) 즉시/보류 분류 후 반영

## Decisions (15:43 추가)
- `spawn docker ENOENT` 장애의 직접 원인을 "sandbox=all 적용 후 Docker 런타임 부재"로 확정.
- 즉시 복구 경로를 2안으로 정리:
  1) Docker 설치 후 sandbox 유지(보안 우선)
  2) 임시 sandbox 완화로 서비스 우선 복구(보안 저하 감수)

## State Changes (15:43 추가)
- 원인 분석 수행 결과:
  - Gateway 로그에서 `Embedded agent failed before reply: spawn docker ENOENT` 반복 확인(15:14~15:16 구간)
  - 시스템 확인 `command -v docker` 결과 `DOCKER_NOT_FOUND`
- 결론: 이전에 적용한 `agents.defaults.sandbox.mode="all"` 설정이 현재 호스트(도커 미설치)와 충돌.

## Lessons Learned (15:43 추가)
- 보안 강화 설정(sandbox=all)은 런타임 전제(도커 설치 여부)와 반드시 함께 점검해야 함.
- 설정 변경 후에는 기능 테스트뿐 아니라 "실행기 존재 여부(command -v docker)"를 즉시 검증해야 연쇄 장애를 줄일 수 있음.

## Blockers (15:43 추가)
- Docker 미설치 상태로 인해 sandbox=all 환경에서 에이전트 실행 불가.
- 따라서 현재는 "보안 강도 유지"와 "즉시 가동" 사이의 트레이드오프 의사결정 대기.

## Active Tasks / WIP (15:43 추가)
1) Docker 설치 여부 결정 및 실행(설치 시 sandbox=all 유지)  
2) 또는 임시 sandbox 완화 패치로 즉시 복구  
3) 복구 후 security audit 재검증 및 경고/critical 재평가

## Decisions (15:49 추가)
- 사용자 선택에 따라 복구안 `2` 채택: Docker 설치 전까지 sandbox를 임시 완화(off)해 서비스 가용성을 우선 확보.
- 운영 방침: 단기 가동성 확보 후, 추후 Docker 설치 + sandbox 재강화로 보안/안정 균형 회복.

## State Changes (15:49 추가)
- Gateway config patch 적용:
  - `agents.defaults.sandbox.mode`: `all` → `off`
  - `tools.deny = ["group:web", "browser"]`는 유지
- 패치 후 게이트웨이 재시작(SIGUSR1) 성공, 서비스 상태 running 확인.
- PCM 재확인 결과:
  - `spawn docker ENOENT` 최근 로그 재발 없음
  - OpenClaw status 정상
  - security audit는 critical 1건(small model + sandbox off) 잔존

## Lessons Learned (15:49 추가)
- 보안 설정 변경은 런타임 종속성(Docker) 부재 시 즉시 장애로 이어질 수 있어, "강화 적용 → 실행 가능성" 동시 검증이 필수.
- 장애 시에는 임시 완화로 서비스 복구 후, 재강화 계획을 명시적으로 남겨두는 것이 운영 안정성에 유리.

## Blockers (15:49 추가)
- Docker 미설치로 인해 sandbox=all 재적용 불가(현재 환경 기준).
- security audit critical 1건은 Docker 기반 재강화 전까지 구조적으로 잔존.

## Active Tasks / WIP (15:49 추가)
1) Docker 설치 계획 수립 및 적용(가능 시 sandbox=all 재전환)  
2) 재전환 후 `openclaw security audit --deep` 재검증  
3) captures 원천 로그 생성 경로 복구(별도 미해결)

## State Changes (15:50 추가)
- 사용자 알림("15:45 gateway port 18789 not listening")에 대해 즉시 PCM 재검증 수행.
- 현재 상태 확인 결과:
  - `openclaw status`: gateway running
  - `ss -ltnp`: `127.0.0.1:18789` LISTEN 확인(pid 1390085)
  - watchdog 수동 실행: `status=ok`, `fail_count=0`
- 결론: 15:45 알림은 일시 구간 이벤트(단발)로 판단, 현재는 정상 복구 상태.

## Lessons Learned (15:50 추가)
- watchdog 알림은 단일 시점 스냅샷일 수 있으므로, 즉시 PCM 3종(status/port/watchdog-state) 교차검증이 유효.

## Active Tasks / WIP (15:50 추가)
1) watchdog 오탐 가능성 모니터링 지속(health/ready probe 기반으로 추세 확인)  
2) Docker 도입 여부 결정 후 sandbox 재강화 여부 확정  
3) captures 소스 복구 작업 재개

## Decisions (21:47 추가)
- 사용자 확인("응")에 따라 저녁 회고 크론(Job ID: `1f81c366-2df7-4d4d-a866-8e500674ce09`) 안정화 조치를 즉시 적용.
- 원인 분석 기준을 "단일 실패"가 아닌 "연쇄 실패(codex timeout → fallback auth/model 실패)"로 운영.
- 18800 포트 충돌 이슈는 OpenClaw 자체가 아닌 `growth-center.service` 점유를 우선 원인으로 확정.

## State Changes (21:47 추가)
- 저녁 회고 크론 패치 완료:
  - `payload.timeoutSeconds = 300`
  - `payload.thinking = "low"`
- 동일 크론 강제 실행(`runMode=force`) 후 최근 실행 정상 완료 확인.
- 인증/모델 경로 재점검 결과:
  - `group-sonnet` 기준 fallback provider(`zai`, `anthropic`) 인증 부재 상태 확인.
  - Ollama는 로컬이 아닌 원격(`100.116.158.17:11434`) 사용 중이며, fallback 지정 `qwen2.5:14b`는 원격 목록에 없음(`model_not_found` 재현 가능).
- 포트 18800 리스너 점검:
  - `growth-center.service`(ExecStart: `node server.js`, 기본 PORT 18800)가 127.0.0.1:18800 점유 중임을 확인.

## Lessons Learned (21:47 추가)
- 크론 장애는 모델 1차 실패 이후 fallback 체인(인증/모델 존재)까지 함께 검증해야 재발을 줄일 수 있음.
- "로컬 LLM" 표현은 실제로 원격 Ollama 엔드포인트를 쓰는 구성과 혼동되기 쉬우므로, 모델 존재 확인은 반드시 원격 `/api/tags` 기준으로 해야 함.
- 브라우저/릴레이 포트 충돌은 서비스 유닛의 기본 포트 상수(`PORT || 18800`)를 먼저 확인하면 원인 분리가 빠름.

## Blockers (21:47 추가)
- `group-sonnet` fallback 인증 미구성(`zai`/`anthropic`) 상태 지속.
- fallback 모델로 설정된 `ollama/qwen2.5:14b`가 실제 원격 Ollama에 미설치.
- 18800 포트 고정 점유로 타 구성(브라우저 프로필/릴레이)과 충돌 여지 존재.

## Active Tasks / WIP (21:47 추가)
1) fallback 체인 정리(사용 가능한 provider/model만 남기기)  
2) `group-sonnet` 인증 프로필 정비 또는 fallback 제거 정책 결정  
3) 18800 포트 분리(서비스 포트 재할당 또는 충돌 주체 분리)

## Decisions (22:03 추가)
- 사용자 요청("1 2 3 순서대로")에 따라 우선순위를 아래로 고정:
  1) group-sonnet/ralph auth 동기화
  2) Ollama fallback 모델 정합화
  3) 포트 충돌 감지 로직 추가(미완)
- `All models failed (4)` 원인을 단일 원인이 아닌 **복합 원인**으로 확정:
  - 상위 provider 3개 타임아웃(openai-codex/zai/anthropic)
  - fallback `ollama/qwen2.5:14b` 모델 미존재(404)

## State Changes (22:03 추가)
- auth 동기화 실행:
  - `/root/.openclaw/agents/main/agent/auth-profiles.json` → `group-sonnet`, `ralph` 복사
  - `/root/.openclaw/agents/main/agent/auth.json` → `group-sonnet`, `ralph` 복사
- 인증 경로 검증:
  - `zai_api_key` 직접 호출 테스트 HTTP `200` 확인.
- 모델 참조 교체:
  - `/root/.openclaw/openclaw.json`의 `qwen2.5:14b` 참조를 `qwen3:14b`로 교체
  - `main/group-sonnet` agent `models.json` 교체 완료
  - `ralph` agent `models.json`에는 `qwen2.5:14b` 참조 1건 잔존 확인(미정리)
- 로그 근거 확인:
  - 12:57 구간에서 config reload 이후에도 실행 중 런이 기존 fallback(`qwen2.5:14b`)로 종료되어 404 발생.

## Lessons Learned (22:03 추가)
- 동적 reload가 적용돼도 **이미 시작된 런**은 구 fallback/model을 계속 쓸 수 있어, 재시작 전후 실패 로그를 분리해서 봐야 함.
- 모델 교체 작업은 `openclaw.json`만이 아니라 **agent별 models.json 전체**를 함께 점검해야 잔존 참조를 막을 수 있음.
- 장애 메시지 `All models failed`는 보통 단일 장애가 아니라
  "상위 타임아웃 + 하위 fallback 불일치" 조합일 가능성이 큼.

## Blockers (22:03 추가)
- `ralph` agent 모델 설정에 `qwen2.5:14b` 잔존 참조 1건.
- 브라우저 포트 충돌(18800) 감지/자동 회피 로직은 아직 미적용.
- 외부 LLM 타임아웃(120s) 재발 가능성은 여전히 남아 있음(네트워크/공급자 상태 의존).

## Active Tasks / WIP (22:03 추가)
1) `ralph` agent `models.json`의 `qwen2.5:14b` 완전 제거 또는 실제 모델 설치 중 택1 확정  
2) cron 주요 잡 2개(MACD/시장급변) 강제 실행으로 fallback 경로 재검증  
3) 18800 충돌 감지/회피 로직 추가(사전 `ss/lsof` 체크 + 대체 포트 선택)

## Decisions (22:09 추가)
- 사용자 요청("2.5 제거 + 타임아웃 시 더 빠른 전환")에 따라 즉시 반영:
  1) `qwen2.5:14b` 참조 전면 제거
  2) 모델 실패 전환 속도 향상을 위해 기본 timeout 단축 + fallback 순서 재정렬
- 전환 전략은 "빠른 생존성" 우선으로 확정:
  - `timeoutSeconds`를 120s → 75s로 축소
  - fallback을 `ollama/qwen3:14b` 우선으로 이동

## State Changes (22:09 추가)
- config schema/get 확인 후 gateway config patch 적용 및 재시작 완료:
  - `agents.defaults.timeoutSeconds = 75`
  - `agents.defaults.model.fallbacks = ["ollama/qwen3:14b", "zai/glm-5", "anthropic/claude-sonnet-4-6"]`
  - `agents.defaults.subagents.model.fallbacks`도 동일 순서로 조정
- agent별 모델 잔존 참조 정리:
  - `ralph`의 `models.json`에서 `qwen2.5:14b` → `qwen3:14b` 교체
  - 추가 점검 중 `opus`, `glm`, `main` agent `models.json`의 `qwen2.5:14b`도 함께 교체
- 검증 결과:
  - `/root/.openclaw/openclaw.json` + `/root/.openclaw/agents/*/agent/models.json` 기준 `qwen2.5:14b` 참조 0건
  - `openclaw status` 정상, 재시작 후 설정 반영 확인

## Lessons Learned (22:09 추가)
- fallback 순서는 defaults만 바꿔도 충분하지 않을 수 있어, `agents.list` 개별 agent override도 함께 정렬해야 실제 런타임 동작이 일관됨.
- 과거 세션 jsonl에 남는 `qwen2.5:14b` 문자열은 히스토리 데이터이며, 현재 활성 설정 문제와 구분해서 해석해야 함.
- config 변경은 `config.patch`(검증+재시작)와 파일 직접 수정이 섞이면 드리프트 위험이 있으므로, 최종 상태를 `openclaw status + grep`로 반드시 이중 검증해야 안전함.

## Blockers (22:09 추가)
- 18800 포트 충돌 감지/자동 회피 로직은 아직 미구현.
- 빠른 fallback 정책 적용 후 실제 cron 경로에서 체감 개선 검증(강제 실행 테스트)이 아직 미완.

## Active Tasks / WIP (22:09 추가)
1) MACD/시장급변 cron 강제 실행으로 "75초 타임아웃 + 새 fallback 순서" 실전 검증  
2) 18800 충돌 감지/회피 로직 추가(사전 점유 체크 + 대체 포트 전략)  
3) 필요 시 `agents.list`의 개별 fallback 순서와 defaults 순서 재동기화 유지

## Decisions (22:11 추가)
- 원격 Ollama 모델 `nomic-embed-text:latest`는 **임베딩 전용**으로 분류.
- 운영 결정: fallback/에이전트 응답용 모델 후보에서 제외하고, 필요 시 memory/RAG 임베딩 용도로만 사용.

## Lessons Learned (22:11 추가)
- Ollama 모델 목록에 있다고 해서 모두 생성형(chat/completions)으로 사용할 수 있는 것은 아님.
- 모델 검토 시 "생성형 vs 임베딩" 타입 구분을 먼저 해야 잘못된 fallback 편입을 예방할 수 있음.

## Blockers (22:11 추가)
- 없음(해당 질의는 모델 타입 확인 이슈로 정리 완료).

## Decisions (22:12 추가)
- `nomic-embed-text:latest`를 실제 활용 대상으로 확정하되, 용도를 **RAG 임베딩 전용**으로 제한.
- OpenClaw 기본 `memory_search` 경로에는 직접 연결하지 않고, 별도 커스텀 파이프라인(임베딩→벡터저장→검색)으로 붙이기로 방향 설정.

## State Changes (22:12 추가)
- config schema 확인으로 `agents.defaults.memorySearch.provider` 지원값(openai/local/gemini/voyage/mistral) 재검증.
- 결론: 현재 스키마상 `provider: ollama` 미지원 확인(직결 불가).
- 다음 구현안 초안 합의:
  1) 문서 청킹
  2) Ollama `/api/embeddings`(nomic) 호출
  3) 벡터DB(Chroma/FAISS) 저장
  4) top-k 검색 결과를 생성형 모델 컨텍스트로 주입

## Lessons Learned (22:12 추가)
- "모델 사용 가능"은 두 축(생성 API 경로, 임베딩 API 경로)으로 분리해 판단해야 함.
- 스키마 제약을 먼저 확인하면 불필요한 config patch 시행착오를 줄일 수 있음.

## Blockers (22:12 추가)
- OpenClaw 기본 memory_search가 ollama 임베딩 provider를 직접 지원하지 않아 즉시 내장 전환 불가.

## Active Tasks / WIP (22:12 추가)
1) `scripts/rag_ollama_nomic.py`(index/query) 구현 여부 사용자 최종 승인 대기  
2) 벡터 저장소 경로(`rag_db/`)와 대상 소스(memory/*.md) 범위 확정  
3) 구현 후 샘플 질의로 검색 정확도/지연 검증

## Decisions (22:13 추가)
- 사용자 지시("이건 나중에 쓰자")에 따라 `nomic-embed-text` 기반 커스텀 RAG 구현은 **지금 즉시 진행하지 않고 보류**로 확정.
- 단기 운영은 기존 OpenClaw `memory_search` 경로 유지.

## State Changes (22:13 추가)
- 실행 계획 상태 전환: RAG 구현 task를 active 실행에서 backlog(나중)로 이동.

## Active Tasks / WIP (22:13 추가)
1) 즉시 착수 항목에서 RAG 구현 제외(보류)  
2) 우선순위 복귀: cron fallback 실전검증 + 18800 포트 충돌 감지/회피 로직

## Decisions (22:15 추가)
- 비용 절감 운영 방향을 "로컬 우선 + API 조건부 승격"으로 정리.
- 사용자 의도 확정: OpenAI API를 기본으로 쓰기보다, 로컬 Ollama 모델을 최대 활용하고 부족한 구간만 API 사용.
- 다음 액션은 5개 로컬 모델 벤치 결과 기반으로 fallback 정책(저비용/균형/품질우선) 재설계.

## State Changes (22:15 추가)
- 5개 모델 비교 테스트를 위한 원격 Ollama 벤치 스크립트 실행 시작:
  - 대상: `qwen3.5:35b-a3b`, `exaone-deep:7.8b-q8_0`, `qwen3:14b-fast`, `qwen3.5:35b-fast`, `qwen3:14b`
- 테스트 실행 세션(`salty-atlas`)은 사용자 질의 응답 우선으로 중단(kill)되어 결과 미수집 상태.

## Lessons Learned (22:15 추가)
- 비용 최적화 대화 중에는 즉시 config 고정보다 "모델 성능/지연 측정 → 정책 확정" 순서가 안전함.
- 장시간 벤치 명령은 진행 중 사용자 질의가 들어오면 중단/재개 전략을 먼저 공유해야 컨텍스트 혼선을 줄일 수 있음.

## Blockers (22:15 추가)
- 5개 모델 실측 벤치 결과가 아직 없어 최종 fallback 재정렬 미확정.

## Active Tasks / WIP (22:15 추가)
1) 5개 로컬 모델 벤치 재실행(정확도/지연/안정성)  
2) 결과 기반 fallback 정책 3안(저비용/균형/품질) 작성  
3) 사용자 선택안으로 `openclaw.json` 반영 후 재시작 및 cron 실전 검증

## Decisions (22:17 추가)
- 사용자 정정에 따라 논의 범위를 "전체 응답 모델 정책"이 아닌 **벡터 서치 API(memory_search) 비용 최적화**로 한정.
- memory_search 비용 절감 기본안은 `provider=local` 중심으로 검토하고, 안정성 확보용 `fallback=openai` 옵션을 병행 제안.

## State Changes (22:17 추가)
- 정책 설명을 memory_search 전용으로 재정렬:
  - 현 상태: `provider=openai (text-embedding-3-small)`로 과금 발생
  - 대안 A: `provider=local` (비용 0)
  - 대안 B: `provider=local + fallback=openai` (실패 시만 과금)
- `nomic-embed-text`는 OpenClaw 기본 memory_search 경로에 직접 연결 불가(`provider: ollama` 미지원)임을 재확인.

## Lessons Learned (22:17 추가)
- "비용 줄이기" 요청은 대상 범위(LLM 응답 vs memory_search 임베딩)를 먼저 고정해야 오해를 줄일 수 있음.
- memory_search 최적화는 모델 fallback 최적화와 별개 트랙으로 관리해야 정책 충돌이 적음.

## Blockers (22:17 추가)
- memory_search를 `local`로 전환할지(`fallback=openai` 포함 여부) 사용자 최종 선택 대기.

## Active Tasks / WIP (22:17 추가)
1) memory_search 전용 2안 비교안 확정:
   - A) local only
   - B) local + openai fallback
2) 선택안 적용(`agents.defaults.memorySearch`) 후 검색 품질/지연 확인
3) 필요 시 chunk/query 파라미터 튜닝으로 추가 비용 절감

## Decisions (22:19 추가)
- 사용자 관심안은 memory_search 기준 **2번(local 우선 + openai fallback)** 으로 수렴.
- 제안 기준치 정리:
  - 2번 기대 비용 절감: fallback 발생률에 따라 대략 80~98%
  - local only는 비용 95~100% 절감 가능하지만 품질/안정성 리스크 증가
- 운영 권장안은 "초기 2번 적용 후 측정 기반 튜닝"으로 유지.

## Lessons Learned (22:19 추가)
- 비용 절감 효과는 절대값보다 `fallback rate`로 추적하는 것이 실무적으로 가장 명확함.
- local-only는 비용 최적화는 강하지만, 한국어 뉘앙스/긴 문맥 질의에서 품질 분산이 커질 수 있어 안전망(fallback) 유무가 체감 품질을 좌우함.

## Blockers (22:19 추가)
- 실제 환경의 fallback율/지연/정확도 실측 데이터 부재(현재는 추정치 단계).

## Active Tasks / WIP (22:19 추가)
1) memory_search `local + openai fallback` 적용 여부 사용자 최종 승인  
2) 적용 시 1주 관측 지표 정의(fallback율, p95 지연, 체감 정확도)  
3) 관측 결과에 따라 local-only 전환 가능성 재평가

## Decisions (22:21 추가)
- memory_search 비용 최적화에서 "로컬 토큰/컨텍스트를 무작정 크게"보다, **검색 폭(top-k/후보 재정렬) 확대**에 예산을 쓰는 방향으로 정리.
- 운영 방향 유지: `local + openai fallback`을 기본으로 두고, 품질 개선은 검색 파라미터 튜닝으로 우선 달성.

## Lessons Learned (22:21 추가)
- 로컬 무료라고 해도 입력 길이를 과도하게 늘리면 품질이 항상 좋아지지 않음(지연 증가·노이즈 증가 가능).
- 성능 개선 우선순위는 "문맥 길이 확대"보다 "후보 검색/재정렬 품질"이 더 효과적.

## Blockers (22:21 추가)
- top-k/재정렬 강도/청크 크기별 실측 벤치 데이터가 없어 최적값 미확정.

## Active Tasks / WIP (22:21 추가)
1) memory_search 적용안 승인 후 검색 파라미터 튜닝 실험(top-k, minScore, candidateMultiplier)  
2) fallback율·p95·정확도 기준으로 1주 관측 설계  
3) (운영 실수 방지) 본 맥락과 무관한 자동 브리핑성 응답이 섞이지 않도록 응답 채널 정합성 점검

## Decisions (22:24 추가)
- 사용자 요청에 따라 memory_search 정책을 **균형형(local 우선 + openai fallback)** 으로 즉시 적용하기로 확정.
- "로컬 최대화"는 무작정 토큰 확대가 아니라 검색 폭(candidate) 확대로 달성한다는 원칙 유지.

## State Changes (22:24 추가)
- gateway `config.patch` 적용 + 재시작(SIGUSR1) 완료.
- `agents.defaults.memorySearch` 변경 반영:
  - `provider`: `openai` → `local`
  - `fallback`: `none` → `openai`
  - `query.maxResults`: `20` → `24`
  - `query.minScore`: `0.08` → `0.07`
  - `query.hybrid.candidateMultiplier`: `4` → `5`
- 결과적으로 memory_search는 로컬 임베딩 우선, 실패/품질 이슈 시 OpenAI 임베딩으로 자동 전환되는 상태가 됨.

## Lessons Learned (22:24 추가)
- 벡터서치 비용 최적화는 모델 교체보다 `provider/fallback` 조합이 1차 레버리지.
- 균형형 전환 시에는 recall 강화를 위한 maxResults/candidateMultiplier 상향이 fallback율 감소에 직접 기여할 가능성이 큼.

## Blockers (22:24 추가)
- 아직 적용 직후 단계라 fallback율/p95/정확도 실측 데이터 없음.

## Active Tasks / WIP (22:24 추가)
1) 24시간~1주 관측: local hit율, openai fallback율, p95 지연 수집  
2) 관측 결과 기반으로 candidateMultiplier·minScore 재튜닝(과다 노이즈 시 보수 조정)  
3) 필요 시 agent별 memorySearch override(`ralph`, `wife`) 정합성 점검

## Decisions (22:25 추가)
- 사용자 요청에 따라 내일 fallback 비율/지연 보고 후, 더 공격적인 로컬 비중 상향 튜닝을 진행하기로 합의.

## State Changes (22:25 추가)
- 점검 리마인더 cron 1회성 잡 생성:
  - job: `fallback-latency-report-check`
  - 실행시각: 2026-03-03 10:00 KST
  - 목적: memory_search 운영지표(fallback율, p95, 체감 정확도) 보고 + 공격형 튜닝안 제시

## Active Tasks / WIP (22:25 추가)
1) 내일 10:00 KST 리마인더 트리거 후 fallback율/p95 보고  
2) 보고 결과 기반 공격형 프로파일 적용(로컬 비중↑, 필요 시 minScore/후보폭 재조정)  
3) 적용 후 품질 저하 여부 모니터링 및 역조정 조건 정의

## Decisions (23:27 추가)
- Discord 스레드 개선 기능은 현재 활성 상태로 확정: thread-session binding, 72시간 컨텍스트 유지, 길드 멘션 기반 응답.
- Agilestory 전량 수집 작업 역할 분리를 사용자 지시대로 확정:
  - GLM: 크롤링/정제/검증 파이프라인 + 누락 체크리스트
  - Codex: 수집 결과 검수/태그 품질 점검/검색 질의 테스트 리포트
- 진행률 보고 주기는 사용자 요청대로 "1시간 1회"로 고정.
- NQ 알림 템플릿은 압축형이 아니라 과거 "확장형(장문)" 포맷으로 복구하기로 결정.

## State Changes (23:27 추가)
- Watchdog 실시간 점검 수행 결과 정상:
  - health watchdog: ok, fail_count=0
  - sessions watchdog: ok, 텔레그램 세션 사용률 약 31%
  - cron-sanitize 최근 1건 정리 확인
- X 링크 파싱 경로 재검증:
  - x.com 직접 파싱은 메타 추출 실패
  - fxtwitter 경유는 본문/이미지 메타 추출 성공
  - vxtwitter/fixvx는 일부 링크에서 실질 본문 추출 실패(리다이렉트/껍데기 응답)
- AC2 수집 작업용 서브에이전트 런 시작 및 지시 반영(체크포인트/재개 가능 설계).
- 1시간 진행률 리마인더 cron 추가:
  - job name: `ac2-agilestory-hourly-progress`
- 서브에이전트 완료 보고 수신:
  - 발견 URL 373, 처리 373(100%), 실패 0
  - 산출 경로: `memory/second-brain/ac2/agilestory/`
  - 보고 파일: `reports/coverage_report.json`, `reports/ingest_done.json`

## Lessons Learned (23:27 추가)
- 모델 장애는 단일 원인이 아니라 동시 복합 장애로 나타날 수 있음(타임아웃 + provider overload + 로컬 abort 동시 발생).
- "All models failed (4)"는 영구 장애가 아니라 순간 스파이크일 수 있으며, 재시도 런이 즉시 정상 복귀하는 패턴이 있음.
- X 링크 자동 추출은 현재 `fx 우선` 전략이 실무적으로 가장 안정적.

## Blockers (23:27 추가)
- NQ 확장형 템플릿 복구는 아직 미완료:
  - 현재 `/root/.openclaw/scripts/nq_macd_multi.py`가 압축형 출력 구조
  - 과거 확장 필드(VIX/SOFR/USDKRW/DXY/ETH/VaR/감성/유동성/종합점수) 재결합 작업 필요
- AC2 파이프라인 1차 수집은 완료됐으나, 사용자 지정 2단계(Codex 검수/태그 품질/검색 테스트)는 아직 미실행

## Active Tasks / WIP (23:27 추가)
1) AC2 2단계 진행: Codex 검수/태그 품질 점검/검색 질의 테스트 리포트 생성
2) AC2 시간당 진행 보고 유지(변경 없으면 짧게 "변경 없음")
3) NQ 확장형 템플릿 복구 구현 + 1회 테스트 메시지 검증 후 실운영 반영
4) 모델 스파이크 재발 시 fallback/timeout 튜닝(특히 Codex/GLM 경로)
