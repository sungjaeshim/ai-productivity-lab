# 2026-03-01

## Decisions
- Discord+Telegram 운영 분리 확정:
  - Telegram = 긴급/승인/결정만
  - Discord = 진행 로그/아카이브/프로젝트 운영
- Discord 프로젝트 구조 확정:
  - `jarvis-hq`, `proj-growth-center`, `proj-second-brain`, `proj-ceo-ai`, `proj-macd-alerts`
- Discord 수신 정책 확정: `groupPolicy=open` + 길드 `requireMention=true`
- Call-Me v1(전화 없음) 구현 방향 확정:
  - L1/L2만 먼저 운영, L3(실전화)는 보류
- Cognitive-Energy 기준값 상향 확정: `active_max 3 -> 5`

## State Changes
- Discord 채널 연동 완료 (봇 `@Jarvis` running/works)
- Discord 설정 대폭 적용:
  - `dmPolicy=pairing`, `historyLimit=60`, `maxLinesPerMessage=17`, `replyToMode=first`
  - `threadBindings.enabled=true`, `ttlHours=72`, `spawnAcpSessions=true`, `spawnSubagentSessions=true`
  - `streaming=partial`, activity/status 설정 적용
  - guild `1477310010395725826`에 `requireMention=true`, `reactionNotifications=own`
- Cron 추가/변경:
  - `🧭 Daily HQ summary reminder` 매일 09:00 KST (`ea3fd51a-1618-4552-81d3-9201d68718a1`)
  - `📏 Cognitive-Energy active_max 재평가` 2026-03-03 09:10 KST (`d785709b-1407-4ec9-9704-a815347b64b2`)
- Growth Center Cognitive-Energy 규칙 수정:
  - `/root/Projects/growth-center/data/cognitive-energy/todoist-hold-rules.v1.json`
  - `active_max=5`, `active_count_gt=5`, `keep_top_n_in_active=5`
  - health 확인: `todayTasks=4`, `overLimit=false`
- Call-Me v1 산출물 생성됨:
  - `scripts/callme-v1-router.js`
  - `scripts/callme-v1-dispatch.sh`
  - `scripts/callme-v1-healthcheck.sh`
  - `scripts/callme-v1-sample-event.sh`
  - `docs/callme-v1.md`, `docs/callme-v1-runbook.md`
- Resend/Sentry 작업 상태:
  - `resend` 의존성 package.json에 정식 고정 (`^6.9.3`)
  - `@sentry/node` 설치/코드 연동 유지 (`^10.40.0`)

## Lessons
- Discord에서 답이 없는 주원인: `groupPolicy=allowlist`일 때 길드 메시지 차단됨.
- Discord 채널 자동 생성은 현재 provider에서 미지원 (`message action=topic-create` 불가).
- Sentry는 DNS 연동 대상이 아니라 앱 내부 DSN 연동 대상.
- Telegram TTS는 "전화"가 아니라 음성 메시지 푸시.

## Blockers / Risks
- Discord 봇 토큰이 대화에 노출됨(사용자가 회전 보류) → 보안 리스크 잔존.
- Resend 실발송/Sentry 실수집은 환경변수 미설정으로 미완:
  - `RESEND_API_KEY`, `RESEND_FROM`, `RESEND_TO`, `SENTRY_DSN`
- Twilio/VoIP 미연동으로 실전화(L3) 미구현.
- memory_search 일부 쿼리 무결과 이슈(투자/러닝) 재발 가능성.

## Active Tasks / WIP
1. Call-Me v1 운영 투입 (L1/L2):
   - 샘플 이벤트/헬스체크는 완료, 실제 운영 트리거 연결은 후속 필요.
2. 2026-03-03 09:10 KST에 Cognitive-Energy `active_max` 재평가 (4/5/6 미세조정).
3. Resend/Sentry 실운영 마감:
   - env 주입 후 실제 발송 1건 + Sentry 이벤트 1건 검증 필요.
4. Cron 오류 대응 잔여 모니터링:
   - 저녁 회고/장마감/Ralph 관련 실패 재발 여부 추적.

## State Changes (11:02 추가)
- 사용자 요청으로 PingWest 링크 백업 저장: https://www.pingwest.com/w/311614
- 용도: GLM 만료/임박 시 대체 참고용 링크.
- 리마인더 생성 완료: 2026-05-25 09:00 KST (Job: ff1a758a-9491-4ea8-8818-7ebb1a7e236f)

## Preferences (12:57 추가)
- 사용자 피드백: 모델/씽킹 운용이 "자꾸 바뀌는 체감"이 강해 불편.
- 운영 원칙 재확인: 메인 세션 모델은 codex로 고정 유지, 씽킹은 기본 medium + 필요 가능성 0.1%라도 있으면 high/xhigh 선제 상향.

## Preferences (12:59 추가)
- `✅ Subagent finished`류 완료 알림 수신 시, 후속으로 자동 `핵심결과 3줄 + 남은 액션` 요약을 즉시 붙여 사용자에게 응답하는 운영 방식 승인.

## Session Update (13:02)

### Decisions
- 메인 세션 운용 고정 재확인:
  - 모델: `openai-codex/gpt-5.3-codex` 유지
  - 씽킹: 기본 `medium`, 필요 가능성 0.1%라도 보이면 `high/xhigh` 선제 상향
- 완료 알림 후속 응답 규칙 확정:
  - `✅ Subagent finished` 수신 시 자동으로 `핵심결과 3줄 + 남은 액션` 즉시 회신

### State Changes
- 사용자 지적("응답이 자꾸 바뀜") 반영해 운용 원칙 재고정 및 메모리 반영 완료
- 무응답 건 원인 확인 완료:
  - 장애/크래시 아님
  - 서브에이전트 완료 알림 이후 후속 멘트 누락(운영 미스)

### Lessons
- `[System Message] Subagent finished`류는 전달만으로 끝내면 체감상 "먹통"으로 인식됨
- 완료 알림 이벤트는 반드시 후속 요약 응답까지 한 트랜잭션처럼 처리해야 함

### Blockers / Risks
- 즉시 신규 blocker는 없으나, 기존 미해결 항목 지속:
  - Cron 오류 3건(저녁 회고/01:00 Ralph Loop/05:10 장마감)
  - Resend/Sentry 실운영 검증 미완
  - Discord 봇 토큰 회전 보류 보안 리스크

### Active Tasks / WIP
- 우선순위 정리:
  1) Cron 오류 3건 원인 분석 및 복구
  2) Resend/Sentry env 주입 + 실발송/실수집 1회 검증
  3) Discord 봇 토큰 회전
- 운영 품질 과제:
  - Subagent 완료 알림 후 자동 후속 요약 규칙 실제 적용/지속 점검

## Decisions (13:11 추가)
- 동일 시스템 경보(RAM/CPU) 10분 내 재발 시, 저우선순위 작업 2개를 즉시 중단하는 대응 규칙 승인.

## Decisions (13:47 추가)
- 사용자 승인으로 모델 전략 vFinal 재적용 확정:
  - main/wife primary=codex
  - group-sonnet/ralph/subagent primary=glm-4.7
  - fallback 순서 고정(클라우드 우선, qwen2.5:14b 마지막)
  - thinkingDefault=medium, 안정화 v2(timeout 120 / bootstrapTotalMaxChars 90000 / compaction 0.45·30000)
- 운영 규칙 강화: 사용자가 먼저 지시하기 전 모델 전략/폴백 설정 임의 변경 금지.

## State Changes (13:47 추가)
- gateway config.patch + 재시작으로 vFinal 복구안 즉시 반영 완료.
- 드리프트 상태(main primary=glm-4.7-flash) 해소, main primary를 codex로 재고정.

## Session Update (14:17)

### Decisions
- 러닝코치 알림 미수신 원인 우선순위 확정:
  1) 서버 장애 여부보다 Strava 원본 활동 업로드/동기화 상태를 먼저 확인
  2) Strava 이벤트 생성 후에 Discord/Telegram 전송 경로를 재검증

### State Changes
- 러닝코치 webhook 서버 점검/정리 후 정상 기동 상태 확인(포트 18790 active).
- 진단 결과, 현재 파이프라인은 "이벤트 대기" 상태로 확인됨(서버 다운 아님).

### Lessons
- 알림 미수신 이슈에서 서버부터 의심하면 시간 낭비 가능성 큼.
- Strava 자동화는 "활동 업로드 → 이벤트 발생 → 전송" 체인이라, 첫 단계 누락 시 하위 채널(Telegram/Discord) 모두 무반응이 정상 동작일 수 있음.

### Blockers / Risks
- 오늘(2026-03-01) 러닝 활동이 Strava API 기준 미확인(최근 러닝 2/22) → 자동 코칭 트리거 불가.
- Discord 자동 업데이트도 Strava 이벤트 의존이라 동일하게 대기 상태.

### Active Tasks / WIP
1. 사용자 측 Strava 앱에서 오늘 활동 업로드/동기화 여부 확인 대기.
2. 활동 확인 후 webhook 수신 및 Telegram/Discord 전송 재검증.
3. 동일 이슈 재발 시 "최근 activity API 확인"을 1차 진단 체크리스트로 고정.

## State Changes (15:02 추가)
- 사용자 요청("디코에 반영")에 따라 X 링크 1건을 brain 파이프라인으로 반영 완료.
  - URL: https://x.com/vadimstrizheus/status/2027953432326197508?s=46
  - Discord 전송 성공:
    - brain-inbox 메시지 ID: `1477546766663421953`
    - brain-ops 메시지 ID: `1477546791674187808`
  - second-brain 기록 갱신: `memory/second-brain/links.md`
- 운영 점검 메모:
  - 기본 환경에서 `BRAIN_INBOX_CHANNEL_ID`/`BRAIN_OPS_CHANNEL_ID` 미설정 시 Discord 전송이 local queue로만 저장됨.
  - 채널 ID 주입 후 즉시 정상 전송 확인.

## Session Distill (20:58 추가)

### Decisions
- Brain 링크 파이프라인 운영 규칙 확정:
  - 기본은 `brain-inbox`만 전송
  - `brain-ops` 스레드는 `--ops-init`일 때만 생성
- CI 실패 감시는 GitHub Secrets 의존 대신 OpenClaw cron 모니터를 우선 사용하기로 확정.
- Resend 운영 전환 정책 확정:
  - 테스트 모드(B안) 먼저 통과
  - 이후 `aisnowball.work` 도메인 검증 후 실운영(`alerts@aisnowball.work`) 전환.

### State Changes
- Git 반영 완료:
  - `b7eb555` feat(brain): ops 스레드 수동 생성(`--ops-init`) 전환
  - `338a5c4` test(brain): `test_brain_link_ingest.sh` 회귀 테스트 추가
  - `c0c3b8d` ci(brain): push/PR + 30분 스케줄 자동 실행 워크플로우 추가
  - `297ca71` ci(brain): 실패 시 Telegram 알림 step 추가
  - `6422a5a` fix(ci): workflow `if`에서 secrets 직접 참조 제거(env 사용)
  - `257ea79` fix(ci): 테스트 스크립트 절대경로 제거(repo-relative)
  - `3dab581` refactor(brain): brain 관련 스크립트 경로를 repo-relative로 정리
- CI 상태 전환:
  - 실패 런(22540875540, 22541969102) 원인 분석/수정 후
  - 성공 런 확인: `22542135872`, `22542173358` (success)
- OpenClaw cron 추가/개선:
  - Job: `🚨 GitHub CI failure monitor (brain-link-ingest-test)` 생성
  - `gh auth` 의존 제거 후 GitHub public API 기반 모니터로 변경
  - 중복 알림 방지 state 파일 적용(`/root/.openclaw/workspace/.state/*`)
- Resend 설정 진행:
  - growth-center env 주입 완료 (`RESEND_API_KEY`, `RESEND_FROM`, `RESEND_TO`)
  - 테스트 모드 발송 성공: `to=mujigi@naver.com` (messageId: `111b0bfb-ef8a-4d5c-8993-3418e843d442`)
  - Resend 도메인 생성 완료: `aisnowball.work` (id: `05745a10-6ac2-42f3-98b1-e5e90f3582b3`)
  - DNS 필요 레코드 확보(DKIM TXT, send MX, send SPF TXT)

### Lessons
- CI 테스트 스크립트에서 절대경로(`/root/.openclaw/workspace`) 하드코딩은 GitHub Actions에서 즉시 장애로 이어짐.
- GitHub Actions 표현식에서 `if:` 조건의 secrets 참조는 파싱 실패 케이스가 있어 `env` 경유가 안전함.
- Resend 테스트 모드에서는 수신자 제약이 강함(본인 인증 메일만 허용) — 운영 수신자 검증에는 도메인 인증이 필수.
- 민감키를 채팅에 노출한 경우 즉시 키 rotate가 기본 대응.

### Blockers / Risks
- Cloudflare DNS 수정용 API 토큰(`Zone DNS Edit`)을 현재 세션에서 확보하지 못함 → DNS 레코드 자동 반영 불가.
- Resend 실운영 전환 전 단계:
  - `aisnowball.work` DNS 레코드 반영 및 Verify 미완
  - `alerts@aisnowball.work` 발신 전환 미완
- 보안 리스크: 기존 노출된 Resend API 키 폐기(rotate) 필요.

### Active Tasks / WIP
1. Cloudflare DNS에 Resend 레코드 3개 반영(DKIM TXT / send MX / send SPF TXT).
2. Resend 도메인 Verify 완료 후 발신자 `alerts@aisnowball.work`로 전환.
3. Growth Center env 재적용(`RESEND_FROM=alerts@aisnowball.work`, `RESEND_TO=mujigi@gmail.com`) 및 실발송 재검증.
4. 노출된 기존 Resend API 키 즉시 rotate 후 새 키로 교체.

## Preferences (21:17 추가)
- Discord/Telegram 브레인 리포트 포맷 선호 업데이트:
  - 묶음 1개 메시지 대신 항목별 "개별 메시지" 전송 선호(DONE/리포트 포함).
- brain-ops 관련 운영 확인 요청:
  - ops 스레드는 사용자가 명시적으로 시작 지시할 때만 생성/활성화(`--ops-init`).

## State Changes (21:19 추가)
- second-brain 링크 정리 실행:
  - 파일: `/root/.openclaw/workspace/memory/second-brain/links.md`
  - 백업 생성: `links.md.bak-pre-clean-20260301-2119`
  - 중복/시스템·민감·잡링크 제거 후 30건 → 12건으로 정리.
- 개별 전송 포맷 반영 완료:
  - `scripts/brain-done-broadcast.sh`를 항목별 개별 메시지 전송 방식으로 수정/검증.

## Blockers / Risks (21:19 추가)
- Telegram 세션에서 Discord로 직접 발송 시 cross-context 정책 차단 발생.
- CLI 우회(`openclaw message send`)도 현재 로컬 config invalid(plugin path missing)로 실패.

## Preferences (03:06 추가)
- 링크 반영 알림 채널 선호 업데이트:
  - 앞으로 brain 링크 반영은 Discord + Telegram 동시 전송을 기본으로 사용.
