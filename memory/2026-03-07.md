# 2026-03-07 Session Distill

## Decisions
- User asked to clarify liquidity notation (`+15`) and requested explicit interpretation.
- Clarified/standardized interpretation: liquidity score (`+15`) is derived from NetLiq delta (`Δ`) vs prior reference point; operationally treated as day-like (전일 대비 성격).
- User requested briefing format update: **liquidity must be shown in one line only**.
- User requested recovery of interrupted KIS Codex CLI work; decided to restore continuity via a single resume handoff doc with actionable next steps.

## State Changes
- NQ briefing stream continued in Discord channel with repeated interval updates.
- Market regime shifted from mixed/long-bias earlier to clear downside dominance:
  - `60m 데드크로스` confirmed around 18:31 KST.
  - `240m 데드크로스` confirmed around 22:16 KST.
  - Multi-TF alignment moved to `0%` (240/120/60/30/15 all red) in late session.
- Price moved from upper-25k zone (~25,1xx) to mid-24k zone (~24,6xx) during session window.
- Risk tone worsened:
  - RSI(30m) dropped into oversold territory (~16–25 range).
  - ADX rose sharply (trend strength expansion).
  - VIX spike signal observed (alert-level move).
- KIS project continuity restored operationally:
  - Snapshot collected (`git status`, recent commits, diffstat, untracked map).
  - Test baseline confirmed (`139 passed, 2 skipped`).
  - Resume document created: `projects/kis-auto-trading/NEXT_SESSION_2026-03-07.md`.

## Lessons
- When trend-strength (ADX) expands while all TF MACD align red, counter-trend bounce signals are low reliability until explicit cross reversal appears.
- Users care about semantic clarity in score labels (e.g., liquidity score vs raw delta); compact labeling reduces confusion.
- Formatting constraints from user should be treated as hard contract in recurring auto-briefings.
- For interrupted coding sessions, fastest recovery is: current snapshot + verified test baseline + explicit P0~P3 resume plan in one handoff file.

## Blockers
- No technical blocker in reporting pipeline identified in this session.
- Ongoing market-side noise/volatility (high VIX regime) remains analysis uncertainty, not system failure.
- KIS live verification remains blocked by `AUTH` dependency if appkey/appsecret/account/market subscription are not fully provisioned.

## Active Tasks / WIP
- Keep one-line liquidity output contract in subsequent NQ briefings.
- Monitor for reversal prerequisites before bias change:
  - 60m golden cross re-entry,
  - reclaim/hold above 50EMA,
  - RSI recovery above 50.
- Continue watching whether full-red alignment relaxes (first at 15m/30m, then 60m) before changing from `대기` stance.
- KIS continuity next steps (from handoff doc):
  - P0: uncommitted work preservation and commit split.
  - P1: dry_run safety verification (command/approval/fallback paths).
  - P2: `docs/PHASE1_CHECKLIST.md` real-progress update.
  - P3: `scripts/test_kis_connection.py` live-precheck after credential provisioning.

---

## Session Distill Addendum (08:02–08:31 KST)

### Decisions
- User requested a consolidated multi-day progress view; decision taken to maintain a single operating checklist as source of truth.
- User selected decision path `2A 3A 4A` (with #1 handled in another channel):
  - **2A**: DDD-based duplication follow-up with prevention gate first.
  - **3A**: MoAI×Codex evidence mapping + completion-rate report.
  - **4A**: timeout conditional auto-pin policy (`2/2h` trigger, `6h` pin, `4h` quiet rollback).
- User confirmed remaining #1 item would be closed in another channel; this channel should close out 2/3/4 only.

### State Changes
- Created and pinned operating board:
  - `reports/ops-checklist-2026-03-07.md`
  - Included status, owner, due, done criteria, and decision lock block.
- Executed D2 and marked DONE:
  - Added duplication gate script: `projects/kis-auto-trading/scripts/check_duplication.sh`
  - Added policy doc: `projects/kis-auto-trading/docs/DUPLICATION_GATE.md`
  - Added CI workflow: `projects/kis-auto-trading/.github/workflows/duplication-gate.yml`
  - Baseline measured: duplicated lines `0.29%`
  - Gate PASS at `0.50%`, verified FAIL at `0.10%` (`EXIT:2`).
- Executed D3 and marked DONE:
  - Created trace report: `reports/moai-codex-trace-2026-03-07.md`
  - Evidence matrix produced (repo/branch/commit/artifact).
  - Completion rate calculated: `80%`.
- Executed D4 and marked DONE:
  - Policy doc created: `docs/ops/TIMEOUT_PIN_POLICY.md`
  - Dry-run simulator created: `scripts/simulate-timeout-pin-policy.sh`
  - Dry-run verified `DRY_RUN_OK` with PIN/ROLLBACK/NOOP scenarios.

### Lessons
- Decision locking (A/B code) reduces cycle time in fast-moving ops threads.
- For duplication refactoring, behavior-preserving DDD + measurable gate threshold is effective (prevents regression without blocking all refactors).
- A short dry-run simulator is sufficient to validate trigger/rollback semantics before enabling operational policy.

### Blockers
- Remaining MoAI×Codex closure is still missing a single final sign-off artifact (`80%` status in trace report).
- KIS live path still depends on `AUTH` prerequisites (credentials/subscription readiness).

### Active Tasks / WIP
- [WIP] #1 decision stream is being closed in another channel; merge closure status back into this checklist when available.
- [WIP] Optional D3 finalization:
  - create `reports/moai-codex-final-signoff-2026-03-07.md`
  - then flip D3 from DONE-with-gap to fully CLOSED semantics.
- [HOLD] Night bundled 30-minute monitoring remains scheduled for sleep-time execution.

---

## Session Distill Addendum (07:42–08:38 KST)

### Decisions
- User approved **A 복구안**: inbox→today 라우팅 파이프라인 즉시 복구(핵심 cron 재활성화).
- User approved **A 주기안(저빈도 균형)**: 자동투입 15분 / 재시도 20분 / 정리 20분 / 모니터 30분.
- User requested one-time catch-up; executed immediate catch-up pass (temporary high read window for Discord autopull).
- User approved **A 규칙 패치** for promotion: p3도 실행의도 힌트 키워드가 있으면 today 승격 허용.
- User requested 3-day backlog re-evaluation report after A patch; completed and reported.

### State Changes
- Diagnosed root state: relevant brain pipeline cron jobs were disabled (`enabled:false`) despite gateway healthy.
- Re-enabled 5 jobs:
  - `🧠 brain 자동투입 (Discord→Router)`
  - `🧠 brain 자동투입 (Telegram→Discord)`
  - `🔁 brain 라우팅 재시도 워커`
  - `🧹 brain 큐 자동정리`
  - `🩺 brain 라우팅 안정화 모니터`
- Updated schedules:
  - Telegram→Discord: `*/15 * * * *`
  - Discord→Router: `2,17,32,47 * * * *`
  - Retry: `6,26,46 * * * *`
  - Autoclean: `8,28,48 * * * *`
  - Health monitor: `9,39 * * * *`
- One-time catch-up executed successfully:
  - Discord autopull pass #1/#2 done
  - route-retry and queue-autoclean drained
  - pending queues remained `0`
- Analyzer patch applied in `scripts/brain-content-analyze.sh`:
  - added `ops_hint_kw`
  - changed `should_ops` to allow `priority==p3 && has_ops_hint`
- 3-day backlog report generated:
  - `reports/inbox_today_backlog_3d.json`
  - result: total 31 link entries, all p3, A-rule ops eligible 0.

### Lessons
- `inbox→today 0건` can be caused by **policy strictness** (promotion threshold), not pipeline failure.
- Recovery sequence that worked: enable jobs → verify status/queues → run one-time catch-up → re-check channel outputs.
- Domain-heavy reference links (X/GitHub) without explicit action language stay p3/general and still won’t auto-promote under A-light rules.
- Chat-channel autopull can over-route conversational mentions into `#todo/#idea` if classifier/routing scope is broad; needs guardrails.

### Blockers
- Main blocker after recovery is semantic/rule mismatch: user expects execution-oriented escalation, but incoming links remain mostly reference-style p3.
- A patch alone did not move 3-day historical links to today (0/31 matched).
- Potential noise blocker: false-positive autopull on general conversation in #today observed during live checks.

### Active Tasks / WIP
- [WIP] Design and apply stricter conversation filter + trigger model (A-prime): action-intent + personal ops context + recurrence signal.
- [WIP] Decide whether to add B-lite domain/pattern promotion for selected sources to avoid persistent 0-inflow.
- [WIP] Validate next 24h metrics after patch:
  - promotion rate (inbox→today)
  - noise rate (non-action false promotions)
  - execution conversion (today items acted on).
- [WIP] If noise persists, tighten autopull input-channel scope and/or require explicit markers for todo escalation.

---

## Session Distill Addendum (07:25–09:10 KST)

### Decisions
- User approved **B안** for nightly maintenance: integrate into existing backup flow + SQLite mirror + conditional alerting.
- User requested quantified impact and reuse-first integration; decision locked to "new cron minimal, reuse existing scripts/jobs".
- User chose **A** for Card4 execution mode: close completed decision items immediately and run remaining checks now.
- User requested Telegram→Discord mirror to post **link-only** format in inbox channel:
  - channel `1478250668052713647`
  - fixed template: `뉴링크` + URL line.

### State Changes
- Implemented nightly memory pipeline:
  - created `scripts/nightly-memory-hygiene.sh`
  - created `scripts/memory_sqlite_mirror.py`
  - integrated nightly hygiene into `scripts/full-backup.sh`
  - generated state files: `.state/nightly-maintenance.json`, `.state/memory_mirror_last.json`
  - created SQLite mirror DB: `data/memory_mirror.db`.
- Added warning-only notification policy for nightly maintenance (PASS is silent; WARN/FAIL sends alert once with dedupe key).
- Identified and fixed false WARN root cause in recall checker:
  - `recall_quick_check.py` header parser accepted only `###`; patched to accept both `##` and `###`.
  - score recovered `60 -> 100` on re-run.
- Card4 operational closure updated in `data/morning-context-2026-03-07.json`:
  - all 3 tasks marked `[DONE]`
  - `active_count` updated to `0`.
- Added/updated cross-channel mirror cron:
  - job `📎 Telegram 분석 Discord 미러` (`3f03a097-c90a-4c0a-a9d4-8afd28ea7734`)
  - now targets Discord inbox channel `1478250668052713647`
  - sends link-only payload with dedupe via `.state/analysis-discord-mirror-last.txt`.
- Diagnosed recent `⚠️ ✉️ Message failed` incidents in MACD cron runs:
  - first message call omitted `action=send` (schema validation fail),
  - immediate retry with `action=send` succeeded,
  - additional non-fatal missing script path observed for `usdkrw_alert.py`.

### Lessons
- Reusing existing cron flow plus small script additions reduced rollout risk versus adding another standalone maintenance cron.
- Parser-format mismatch (`##` vs `###`) can trigger false operational WARN despite healthy data; checkers should be tolerant to heading variants.
- Cross-context send constraints require isolated execution path; direct Telegram-bound cross-send remains blocked by policy.
- For message-tool reliability in agent prompts, explicit `action: send` must be treated as mandatory to avoid intermittent false error states.

### Blockers
- MACD cron prompt still has latent reliability risk until message-call contract is explicitly hardened (`action=send` in instruction examples).
- One referenced dependency path remains inconsistent in some runs (`scripts/usdkrw_alert.py` missing), causing avoidable noise.

### Active Tasks / WIP
- [WIP] Patch all active NQ MACD cron prompts (weekday/monday/saturday variants) to enforce explicit `message(action=send, channel, target, message)` contract.
- [WIP] Resolve `usdkrw_alert.py` path consistency (restore script or update cron command path).
- [WIP] Observe new Discord link-mirror job for first successful "뉴링크 + URL" send in inbox channel and verify dedupe behavior over next cycles.
- [WIP] Keep nightly maintenance alert policy under watch for 1-2 runs to ensure PASS stays silent and WARN/FAIL dedupe works as intended.

---

## Session Distill Addendum (09:26–09:31 KST)

### Decisions
- User requested high-confidence re-verification of "Naver Map API already integrated" claim using memory + filesystem evidence (xhigh check).
- Decision taken to run a comprehensive credential/config sweep (workspace, ~/.openclaw, optional legacy paths) rather than rely on prior quick check.

### State Changes
- Executed mandatory memory recall search first, then deep scans across:
  - runtime config (`gateway config.get`),
  - env variables,
  - `.env*`, `*.json`, `*.yaml`, `*.yml`, `credentials/` patterns,
  - likely key names (`NAVER_MAP`, `X-NCP-APIGW-API-KEY`, `...KEY-ID`, `NAVER_CLIENT_ID/SECRET`).
- Findings refined:
  - No evidence of Naver **Map** API key/config integration in active OpenClaw runtime/workspace.
  - Detected Naver-related artifacts are non-map:
    - `scripts/setup_naver_email.py` (Naver mail IMAP helper),
    - `skills/naver-blog/SKILL.md` (blog API instructions),
    - `naver.me` links / Naver-news cache text.

### Lessons
- Distinguish "Naver-related" from "Naver Map API integrated": presence of Naver email/blog scripts is not proof of map API wiring.
- For key-existence disputes, search must include both memory records and live runtime/config paths to avoid false positives.

### Blockers
- No technical blocker for verification itself.
- Remaining uncertainty is only possible out-of-band secret storage not discoverable in scanned paths; no evidence found in current scope.

### Active Tasks / WIP
- If user wants final closure, run a second-pass secrets-provider path audit (localfile-backed secret store) and report key-name existence only.

---

## Session Distill Addendum (11:26–13:02 KST)

### Decisions
- User confirmed audio handling policy split by file size and execution mode:
  - `<=10MB`: Route A (speed-first, inline handling)
  - `>10MB`: Route B (separated worker/sub-agent style; keep main chat clean)
- User confirmed model preference split for STT:
  - `<=10MB`: speed-first (`small/turbo`)
  - `>10MB`: quality-first (`medium/large`)
- User explicitly requested removal of invalid fallback model `ollama/qwen3.5:9b-q4_K_M-nothink` from main fallback chain; config patch + restart executed.
- User requested domestic news brief optimization for cognitive load:
  - frequency reduced from `3/day` to `2/day` (`08:20`, `18:20` KST)
  - format changed to compact card view
  - ranking by importance with top 5
  - removed bar graph display; switched to traffic-light signal
  - fixed `[경제]` sector prefix and minimal link label (`[link]`).

### State Changes
- OneDrive public-share ingestion validated:
  - PDF link access + download succeeded (`MoAI-Cowork.pdf`, ~62.7MB, 29p)
  - audio link access + download succeeded (`모두으코웍 1.m4a`, ~36.34MB).
- Implemented routing automation scripts:
  - `scripts/audio_route.py` (size-based route/model/timeout planner)
  - `scripts/audio_auto.py` (job start/status + detached worker execution)
  - policy doc: `docs/ops/AUDIO_ROUTING_POLICY.md`.
- Launched real B-route job for large audio:
  - `jobId=7e41c93c8d40`, route B, model medium
  - final status: `failed` with `TIMEOUT` (transcript not produced).
- Updated user preference records in `USER.md` to reflect 10MB split + model preference policy.
- News cron job updated:
  - job id `889954e6-dbfb-4bc3-b91f-4dcacbd242aa`
  - renamed to `(NAVER, 2회/일)`
  - schedule changed to `20 8,18 * * *`
  - payload top count changed to 5.
- News formatter (`scripts/naver_news_brief.py`) iteratively refactored to high-readability format:
  - one-glance summary line
  - importance-ordered list
  - signal-light markers (`🔴/🟡/🟢`)
  - compact clickable link label (`🔗 [link](...)`)
  - removal of long tracking params where possible.

### Lessons
- For long media processing in chat assistants, reliability improves significantly when source transport is external link-based (OneDrive/Drive) rather than direct chat upload.
- Policy declaration alone is insufficient; users perceive completion only after runnable automation + status surface is in place.
- Detached worker mode needs explicit timeout strategy and fallback/retry plan; otherwise large CPU-bound Whisper runs can end in timeout even with correct routing.
- Cognitive-load reduction in recurring briefs requires ruthless compaction:
  - fewer items
  - single-line status cues
  - hidden long URLs
  - stable visual pattern.

### Blockers
- Current B-route run failed with `TIMEOUT` for ~36MB audio under CPU Whisper medium path.
- Sub-agent orchestration for B-route is currently implemented via detached local worker process/state files; not yet unified with `sessions_spawn`-managed lifecycle and completion callbacks.
- Telegram markdown rendering variability may still affect whether `[link](...)` is displayed exactly as intended on all messages.

### Active Tasks / WIP
- [WIP] Add robust retry/fallback for large audio jobs (e.g., auto re-run with chunking or model downgrade after timeout).
- [WIP] Add completion notifier for `audio_auto.py` jobs (main chat update on done/failed with concise code).
- [WIP] Consider true `sessions_spawn` worker orchestration path for B-route to align with session isolation semantics.
- [WIP] Monitor next scheduled NAVER briefs (08:20/18:20) to verify final readability contract in production messages.

---

## Session Distill Addendum (13:27–13:30 KST)

### Decisions
- User approved current news card format and requested mandatory cross-posting of the same outgoing brief to Discord briefing log channel `1477462915509387314`.
- Decision locked: whenever NAVER domestic news brief sends, it must go to both Telegram (`62403941`) and Discord (`1477462915509387314`).

### State Changes
- Updated cron job `889954e6-dbfb-4bc3-b91f-4dcacbd242aa` payload instructions:
  - Normal brief path now sends to both Telegram + Discord.
  - Error path now sends failure notice to both Telegram + Discord.
- Job schedule/name unchanged (`08:20`, `18:20` KST; NAVER 2회/일).

### Lessons
- User preference emphasizes "same message mirrored to Discord briefing log" as an execution contract, not optional reporting.
- For recurring automations, channel fan-out should be enforced at cron payload level to avoid session-context delivery gaps.

### Blockers
- No blocker observed after patch; update applied successfully.

### Active Tasks / WIP
- [WIP] Verify next scheduled run actually lands in Discord channel `1477462915509387314` with identical body.
- [WIP] Continue applying same Telegram+Discord mirroring rule to future briefing-type automations by default when user asks.

---

## Session Distill Addendum (15:14–15:43 KST)

### Decisions
- User confirmed that `Telegram 분석 미러`-style content should **not** be posted in Discord `#briefing-log`.
- Decision locked: `#briefing-log` keeps briefing logs only; Telegram analysis-mirror content must be sent to Telegram only unless explicitly requested otherwise.
- User requested immediate cleanup: remove misposted analysis-mirror messages and leave one concise correction notice.
- User requested `macd-ops` skill draft creation and same-day test, with 5 required blocks:
  1) trigger, 2) output specification, 3) channel guard, 4) failure retry, 5) verification checklist.

### State Changes
- Confirmed misposted messages existed in `#briefing-log`:
  - `1479631422426710270`
  - `1479632376358240267`
- Executed cleanup:
  - deleted both misposted messages,
  - posted one correction notice (`1479633908634095787`) clarifying policy.
- Created new skill file:
  - `~/.openclaw/skills/macd-ops/SKILL.md`
- Verified skill structure includes all 5 requested blocks (local check PASS).
- Verified runtime registration:
  - `openclaw skills check` shows `macd-ops` as Ready.
- Ran MACD script smoke test:
  - `python3 /root/.openclaw/scripts/nq_macd_multi.py --test-mode`
  - output rendered successfully (`✅ 테스트 모드: 템플릿 렌더링 정상`).

### Lessons
- Channel/content-class mismatch is a real operational failure mode even when message delivery succeeds; channel guard rules must be explicit and enforced.
- Quick recovery pattern for chat routing mistakes works well:
  - confirm misroute evidence,
  - remove wrong posts,
  - leave single correction notice,
  - lock future routing rule.
- Skillization improves MACD ops consistency (format/routing/retry/verification) without changing trading signal logic itself.

### Blockers
- `macd-ops` is currently draft-level guidance; cron payloads are not yet fully hard-bound to the new skill contract.
- One pending reliability noise source remains from earlier runs (`message` schema miss / missing `usdkrw_alert.py` path), requiring follow-up hardening.

### Active Tasks / WIP
- [WIP] Align active NQ MACD cron prompts/runs to `macd-ops` contract (`action=send` required, explicit target, verification checklist).
- [WIP] Keep `#briefing-log` strict to briefing-only content; block analysis-mirror phrasing/routing by default.
- [WIP] Resolve `usdkrw_alert.py` path consistency to reduce false warning noise in MACD jobs.

---

## Session Distill Addendum (17:04–17:10 KST)

### Decisions
- User requested immediate RCA for unexpected NQ MACD postings while "cron should not be running now".
- Decision: prioritize scheduler/runtime evidence (cron jobs, run history, gateway logs) over assumption-based diagnosis.
- User clarified expected behavior: Saturday flow should be valid only through early morning window (up to ~07:00 KST), not afternoon/evening.

### State Changes
- Confirmed active NQ MACD job schedules at investigation time:
  - `4d5b8d9a` (weekday): `1,16,31,46 * * * 2-5`
  - `dcb67262` (Mon session): `1,16,31,46 8-23 * * 1`
  - `1434b316` (Sat dawn): `1,16,31,46 0-6 * * 6`
- Despite above schedules, `4d5b8d9a` executed on Saturday afternoon:
  - run around `15:46 KST`
  - run around `17:00 KST`
- Gateway/scheduler evidence showed `cron.run` completion lines with external websocket connections:
  - `conn=25c6a57b…27c3` (15:46 run)
  - `conn=3c30c705…7c54` (17:00 run)
- Around `16:52 KST`, three MACD jobs were updated (`dcb67262`, `4d5b8d9a`, `1434b316`) and timer re-armed for `17:00`, confirming config mutation activity near incident window.

### Lessons
- In this incident, trigger origin matched **external `cron.run` calls via gateway/ws** rather than pure schedule tick execution.
- Day-of-week cron expressions alone are insufficient guardrails if manual/API `cron.run` is available; operational controls need run-origin visibility and access constraints.
- For fast incident triage, the most decisive artifact is the pair: `cron/runs/<job>.jsonl` + `gateway/ws cron.run response logs` with `conn` identifiers.

### Blockers
- Current logs expose `conn` IDs but do not directly identify human/tool owner in the collected view; attribution remains partially unresolved.
- Afternoon unexpected runs can recur until run-source governance is tightened (who can call `cron.run`, from where).

### Active Tasks / WIP
- [WIP] Trace `conn=25c6a57b…27c3` and `conn=3c30c705…7c54` to concrete caller identity (session/client/token) in gateway auth/connection logs.
- [WIP] Add guardrail for NQ MACD jobs: reject/manual-gate `cron.run` outside allowed windows unless explicit override reason is provided.
- [WIP] Preserve Saturday policy contract in runbooks: allow only dawn session window; block afternoon/evening ad-hoc executions by default.

---

## Session Distill Addendum (17:33–17:41 KST)

### Decisions
- User requested immediate root-cause analysis for `All models failed ... timed out`; decision was to diagnose from runtime evidence first (status/logs/connectivity/cron load), not by hypothesis.
- Initial mitigation choice was **B (observe only)**: keep settings unchanged and monitor recurrence window.
- After D2-2 gate report, user switched to **A (immediate load reduction + re-validate)**.
- D2-2 release decision remained **NO-GO** because latency gate/regression checks failed despite quality metrics staying perfect.

### State Changes
- RCA evidence captured:
  - lane queue wait spikes observed (up to ~153s),
  - embedded run timeout observed (`180000ms`),
  - failover chain timed out across 3 models.
- D2-2 evaluator rerun in same environment exposed fixture integrity issue:
  - fixed malformed `data/recall_eval_samples.json` (trailing comma/extra token).
- B-plan follow-up reminder was scheduled then later removed after A-switch:
  - created one-shot reminder job (`f14cad0c...`) and removed it.
- A-plan low-load actions executed by disabling high-frequency jobs:
  - `3f03a097` (Telegram analysis→Discord mirror)
  - `5e7ba4c8` (route retry worker)
  - `80a3884e` (queue autoclean)
  - `96ddcade` (brain autopull Telegram→Discord)
  - `1b2eaed8` (brain autopull Discord→Router)
  - `0291740a` (router health monitor)
- Re-validation runs (`recall_router_eval.py --limit 120 --fail-on-regression`) after A-switch:
  - quality stayed stable (`top3_hit_rate=100`, `route_match_rate=100`, `error_count=0`),
  - latency improved vs first fail (`hybrid_p95: 49.40ms -> ~39.4/39.8ms`) but still above strict gate in repeated runs.

### Lessons
- For timeout incidents, separating **queue saturation** from **provider latency** using lane-wait + embedded-timeout logs gives faster and more reliable triage than model-blame.
- Passing quality metrics does not imply deployability; explicit latency/regression gates are necessary to prevent silent performance regressions.
- Load-shedding can reduce p95 quickly, but short-window measurements can still oscillate; stabilization window + repeated checks are required before GO decisions.

### Blockers
- D2-2 still fails due to latency gate/regression check (`hybrid_p95` and `latency_vs_baseline`), so production cutover is blocked.
- Root-cause attribution for queue pressure remains partial: high-frequency cron pressure is clear, but exact per-job contribution weights are not yet quantified.

### Active Tasks / WIP
- [WIP] Keep A-plan disabled-job set in place temporarily and run another stabilization-window recheck before re-enabling.
- [WIP] Re-run D2-2 gate after cooldown; require both absolute latency pass and baseline-regression pass before GO.
- [WIP] Prepare safe rollback/restore sequence for re-enabling disabled cron jobs in controlled order once gate passes.

# 📋 Daily DONE Summary: 2026-03-07

No completed items for this date.

---
_Generated by brain-daily-done-summary.sh_


---

## Session Distill Addendum (20:07–21:21 KST)

### Decisions
- User accepted **A안(안정성 우선)** for recall-router: keep degraded automation disabled and re-measure later in low-load window.
- User requested immediate action; one-shot low-load recheck scheduled for next morning 06:30 KST (`6b973752-03cf-419e-87d4-cf9fb9b625be`).
- User asked scope clarification (“어디까지 못쓰는가?”); decision locked to: disable only brain routing automation path, keep normal chat/manual ops available.
- User requested practical `/loop` adoption support; delivered reusable KR template pack (10 items) for general day-to-day use.
- User reported Discord missing delivery; decision: treat as routing reliability issue and backfill immediately.

### State Changes
- Confirmed recall-router remeasure result was FAIL on latency regression despite quality stability:
  - `top3_hit_rate` and `route_match_rate` remained 100,
  - `error_count` remained 0,
  - `hybrid_p95` regressed vs baseline (32.30ms -> 36.63ms).
- Re-applied disabled state to high-frequency brain-routing jobs (automation containment):
  - `3f03a097`, `5e7ba4c8`, `80a3884e`, `96ddcade`, `1b2eaed8`, `0291740a`.
- Added one-shot Discord backfill job (`feeac2e7-101e-4c56-95ec-8bab0a62bdee`) and verified actual Discord post landed:
  - channel `1477462915509387314`
  - message id `1479816034428129523`.
- Patched recurring evening review job (`1f81c366-2df7-4d4d-a866-8e500674ce09`) to reduce delivery ambiguity:
  - switched to `delivery.mode=none`,
  - explicit dual `message send` requirement (telegram + discord) in payload.

### Lessons
- In telegram-bound main context, direct Discord send can fail with cross-context guard; reliable workaround is isolated execution path/job-based send.
- "delivery=announce" confirms one channel delivery but does not guarantee multi-channel fanout unless payload explicitly sends both targets.
- For operational GO/NO-GO, quality metrics (100% hit/match, 0 errors) are insufficient when latency regression gates fail.

### Blockers
- Primary blocker: cross-context messaging constraints when attempting provider switch (telegram -> discord) from bound context.
- Recall-router remains blocked for re-enable due to latency regression gate (performance, not correctness).

### Active Tasks / WIP
- [WIP] Run scheduled low-load recall-router remeasurement at 06:30 KST and decide `keep disabled` vs `canary 1 job`.
- [WIP] On next evening review cron run, verify dual-channel receipt (Telegram + Discord) with IDs and keep payload contract strict.
- [WIP] If Discord miss recurs, split reporting into channel-specific isolated jobs rather than mixed announce semantics.

## Session Distill Addendum (22:20–22:30 KST)

### Decisions
- User requested Christopher Alexander / Nature of Order corpus to be stored in AC2 second-brain "book" format and always retrievable via memory search paths.
- Implemented as AC2 Book Pack cards (CA-001~CA-009), including NOO Vol.1~Vol.4 expanded paragraphs and action cards.

### State Changes
- Created path: `memory/second-brain/ac2/books/christopher-alexander-nature-of-order/`.
- Added files: `00_master-index.md`, `01~09` card files.
- Updated AC2 index: `memory/second-brain/ac2/README.md` with `12-book-packs.md` entry.
- Added index page: `memory/second-brain/ac2/12-book-packs.md`.
- Added retrieval pointers in `memory/second-brain/insights.md`, `memory/second-brain/curated-reads.md`, `memory/second-brain/work-memos.md`.

### Active Tasks / WIP
- [WIP] If user approves, continue next pack depth: per-volume deep-dive cards (chapter-level) and cross-links to AC2 NOO (`06-noo.md`).

## Session Distill Addendum (22:30–22:39 KST)

### Decisions
- User reported "not visible" and requested end-to-end release so the AC2/NOO content is actually searchable from their second-brain surface.
- Decision taken to ship a visibility-first release path: patch fallback dataset + deploy + post-deploy endpoint verification.
- Expanded Book Pack scope to include execution bridge card `CA-010` (NOO 4권 → AC2 실행 맵).

### State Changes
- Book pack extended from `CA-001~CA-009` to `CA-001~CA-010`.
- Added file: `memory/second-brain/ac2/books/christopher-alexander-nature-of-order/10-noo-volumes-to-ac2-execution-map.md`.
- Updated indices:
  - `memory/second-brain/ac2/books/christopher-alexander-nature-of-order/00_master-index.md`
  - `memory/second-brain/ac2/12-book-packs.md`.
- Patched growth-center fallback snapshots directly so deployed API can expose the new data:
  - `growth-center/data/second-brain-data.json`
  - `growth-center/public/data/second-brain-data.json`
  - ensured `ac2=13`, `books=1`, and included Christopher Alexander/NOO payload.
- First release attempt hit build-phase `SIGTERM`; immediate retry succeeded.
- Production deploy completed and aliased:
  - `https://growth.aisnowball.work`
- Post-deploy checks passed:
  - second-brain dashboard success,
  - AC2 endpoint non-empty (`total=13`, source json-fallback),
  - AC2 tab present in `second-brain.html`.

### Lessons
- "파일 저장 완료"와 "사용자 화면에서 검색 가능"은 별개다; fallback snapshot/serving path까지 갱신해야 가시성이 확보된다.
- Release reliability improves by explicit post-checks against target endpoints, not just successful deploy logs.
- For this stack, AC2 list visibility is currently guaranteed via `ac2` fallback payload; dedicated `books` exposure may still depend on UI/API wiring.

### Blockers
- `memory_search` ranking still does not reliably surface the newly added AC2 book-pack docs as top hits immediately (retrieval quality/ranking issue, not storage failure).
- AC2 endpoint currently returns core entries from fallback (`ac2=13`); dedicated book-pack discoverability may require additional API/UI mapping if user wants direct "books" tab behavior.

### Active Tasks / WIP
- [WIP] Improve retrieval discoverability for new AC2 book-pack docs (query boosting/path weighting for `ac2/books/christopher-alexander-nature-of-order`).
- [WIP] If requested, wire/verify dedicated books-category exposure in growth-center API/UI (beyond AC2 core list).
- [WIP] Collect user-side verification result after cache refresh and tune query aliases accordingly.
