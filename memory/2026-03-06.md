# 2026-03-06 Session Distill

## Reconfirm
reconfirm: NQ Telegram output keep full-format and improve readability
reconfirm: Telegram output must avoid Markdown tables; use block/bullet formatting
reconfirm: Direct message send to Discord from Telegram-bound session can hit cross-context denied
재확인: CI failure diagnosis should use exact run logs

## Decisions
- User selected **B안** for AC2 visibility issue: immediate routing patch instead of manual/keep-as-is flow.
- Confirmed policy: AC2-related messages should land in AC2 path, not only generic `ideas.md`.

## State Changes
- Patched `scripts/brain-tag-route.sh`:
  - accepted tag set expanded to include `#ac2`
  - `#ac2` route target added: `memory/second-brain/ac2/13-inbox.md`
  - auto-mirror added: `#idea` containing `ac2|세컨드브레인|second brain|제2의 뇌` also appends into AC2 inbox
  - header comment updated accordingly
- Validation completed:
  - shell syntax check passed (`bash -n`)
  - dry-run checks passed for `#ac2` and `#idea` mirror path
  - live write checks passed; AC2 inbox file created and entries appended

## Lessons
- Root cause was routing design mismatch (ideas-only ingest), not ingest failure.
- Fastest fix was router-level support (`#ac2` + keyword mirror) rather than post-hoc manual promotion.
- Dry-run + one live append is sufficient for confidence before broader backfill.

## Blockers
- None technical after patch.
- Minor side effect: validation produced test entries (and Notion pages) that may need cleanup later.

## Active Tasks / WIP
- Optional WIP (not yet executed): backfill recent AC2-related historical items (today/yesterday or wider window) into `ac2/13-inbox.md`.
- Monitor next real user messages to confirm AC2 visibility in normal flow without manual tagging.

---

## Session Distill (Growth Center incident handling)

### Decisions
- User chose **A안(즉시 복구 + 원인규명/재발방지)** over temporary UI-only mitigation.
- User approved next phase (`다음`): connect host metrics source + clear health warnings to green.

### State Changes
- External incident triage completed for `https://growth.aisnowball.work`:
  - `index.html`/core APIs reachable (HTTP 200).
  - Detected dashboard-reported `disk 100%` as runtime metric artifact.
- Patched and deployed `routes/system.js`:
  - In serverless runtime, disk now reports `N/A` (`note: serverless-unavailable`) instead of misleading `df` value.
  - Added `remoteUnavailableReason` when `SYSTEM_METRICS_URL` is unset.
- Added host metrics snapshot path:
  - New script: `scripts/build-host-system-metrics.js` (writes `public/data/host-system-metrics.json`).
  - `release-prod.sh` now generates host snapshot during release.
  - Vercel env set: `SYSTEM_METRICS_URL=https://growth.aisnowball.work/data/host-system-metrics.json`.
- Health policy normalization patch (`routes/health.js`) deployed:
  - `dailyLog`: fallback recent data (`<=2d`) counts as `ok`.
  - `agents`: no sessions file in runtime treated as `ok` with note.
  - `improvement`: `plan-fallback` with active rows treated as `ok`.
- Production release completed successfully; post-check result:
  - `/api/health/dashboard` => `overall: ok`, `warnCount: 0`, `failCount: 0`.

### Lessons
- `model_context_window_exceeded` was triggered by oversized diagnostic output in-session (broad `rg` dump). Output capping is mandatory.
- Serverless `df` values can reflect container/platform layers and cause false incident signals for app health.
- Health gates should distinguish “feature absent in runtime context” from true service degradation.

### Blockers
- One release attempt was blocked by release guard due to uncommitted critical file (`routes/system.js`); resolved by commit then redeploy.

### Active Tasks / WIP
- Optional follow-up: switch host metrics from deploy-time snapshot to periodic refresh (cron/heartbeat) for fresher CPU/disk visibility.
- Optional follow-up: 30-minute post-release runtime observation + UI capture-based verification.

---

## Session Distill (AC2 tab render/drift incident)

### Decisions
- User decision: **재배포 먼저 실행 + 재발방지 원인규명까지 즉시 완료**.
- Deployment policy decision: 앞으로 임시 `vercel --prod` 직접 실행 대신 `npm run release:prod` 표준 경로 우선.
- UX decision: AC2 화면은 카드형보다 **모바일 우선 컴팩트 리스트형**으로 변경.

### State Changes
- Incident triage completed:
  - `/api/second-brain/ac2` returned data normally (API healthy).
  - `second-brain.html` deployed artifact was drifted/stale vs local latest UI logic.
- Production restored via redeploy; alias updated to `https://growth.aisnowball.work`.
- Release guardrails implemented and committed:
  - `scripts/release-prod.sh`: critical uncommitted file gate added.
  - `scripts/check-release-endpoints.js`: AC2 checks added
    - `second-brain.html` contains AC2 render markers (`sectionDefs`, AC2 tab)
    - `/api/second-brain/ac2` non-empty + core/content/report counts validated
  - `.githooks/pre-commit` enabled (`core.hooksPath=.githooks`) to surface critical-file risk before commit.
  - `docs/deployment-guide.md` added (incident + safe release runbook).
- Additional UI patch applied and redeployed:
  - AC2 cards switched to compact row/list presentation for better mobile readability.
  - Preview text cleaned/shortened; title wrapping improved.
- Commits pushed:
  - `9d3c598` (safe release + AC2 release checks)
  - `50f4fe4` (AC2 card readability)
  - `64e281a` (AC2 compact list rows)

### Lessons
- Root cause pattern: API 정상이어도 **배포 artifact drift**로 UI만 고장날 수 있음.
- `vercel --prod`는 워킹트리 상태 영향을 강하게 받으므로, release script + guardrail 없으면 재발 위험 큼.
- Post-deploy checks must include **UI marker checks + endpoint data shape checks**, not only health endpoint status.

### Blockers
- First formal release attempt intentionally blocked by new gate (critical files uncommitted); resolved by committing critical files before rerun.

### Active Tasks / WIP
- Optional: Vercel dashboard side root/build settings audit to ensure Git-linked deployments follow same release contract.
- Optional: AC2 section folding (accordion) as next readability improvement if user requests.

---

## Session Distill (LLM timeout incident + fallback policy change)

### Decisions
- User asked root-cause analysis for repeated "All models failed" incidents in Telegram direct chat.
- User decision: **B안 적용** — remove `qwen3.5` from all fallback paths **and** remove `qwen3.5` model alias/definition.
- User decision: keep memory embedding model unchanged (`embeddinggemma:latest`).

### State Changes
- Incident diagnosis updated from "auth-only" to **runtime timeout saturation**:
  - latest failures were dual timeout (`openai-codex` + `ollama/qwen3.5:35b-a3b`), with lane wait spikes and cron/memory-query timeout signals.
- Config updates applied (gateway config patch + restart):
  - removed `ollama/qwen3.5:35b-a3b` from all agent fallback chains
    - `agents.defaults.model.fallbacks`
    - `agents.defaults.subagents.model.fallbacks`
    - `agents.list` for `main/group-sonnet/ralph/wife`
  - removed alias block `agents.defaults.models["ollama/qwen3.5:35b-a3b"]`
- Additional final cleanup applied directly to config file + gateway restart:
  - removed `models.providers.ollama.models` entry for `qwen3.5:35b-a3b`
- Verification completed:
  - no remaining `qwen3.5:35b-a3b` references in config
  - memory embedding model still `embeddinggemma:latest`

### Lessons
- Similar "All models failed" messages can mask different root causes over time (auth errors earlier, pure timeout saturation later).
- For degraded local/remote Ollama paths, removing a flaky fallback can reduce tail-latency cascades and queue buildup.
- `config.patch` merge behavior may require an extra explicit cleanup pass for nested provider model arrays.

### Blockers
- Ongoing intermittent timeout pressure observed during this window:
  - codex request timeout,
  - ollama generate timeout (no payload within timeout window),
  - memory embedding query timeout.

### Active Tasks / WIP
- Run immediate post-change smoke checks from Telegram to confirm failure rate drop.
- If timeout persists, temporarily pin primary to `zai/glm-4.7` for stability while codex/ollama latency is investigated.

---

## Session Distill (Growth-center deploy recurrence prevention hardening)

### Decisions
- User chose **A안**: full root-cause trace + recurrence prevention (not just “currently READY” closure).
- Root-cause conclusion fixed: failure was caused by **partial commit** where `package.json` referenced missing scripts.

### State Changes
- Root-cause timeline confirmed:
  - problematic commit: `576f459` (script references added while files missing)
  - recovery commit: `b0a4b6a` (missing scripts added)
- Hardening implemented in `growth-center`:
  - `scripts/validate-required-scripts.js` upgraded to dependency-aware entrypoint validation (`vercel-build`, `release:prod`).
  - `scripts/release-prod.sh` updated to run script-integrity preflight first.
  - commit pushed: `a1a3582` (ops(scripts) hardening).

### Lessons
- Build-breaking drift can originate from **config/implementation split commits** even when latest deploy is green.
- Validation must run at multiple layers (preflight + build path), and scope should target deployment entrypoints to avoid noisy false failures.

### Blockers
- No remaining technical blocker after hardening and push.

### Active Tasks / WIP
- Optional: promote local `.git/hooks/pre-commit` guard into repo-managed hook strategy for team-wide enforcement.

---

## Session Distill (Response format contract upgraded to DVMR)

### Decisions
- User requested permanent response contract upgrade:
  - keep high-signal structured outputs,
  - reduce mechanical/template tone,
  - always include **판단근거**.
- Final contract accepted conceptually as **DVMR**:
  - Definition (완료 정의 1줄), Visibility, Measurability, Reasoning.

### State Changes
- Preference docs updated:
  - `working-preferences.md` top rule expanded to DVMR (added explicit reasoning block + metric/VERIFY rules).
  - `USER.md` updated with durable preferences:
    - pre-task “완료 정의 1줄” first,
    - numeric ops metrics (`완료율`, `재작업률`) when possible,
    - always include 판단근거 for important recommendations.

### Lessons
- User accepts strict information density, but rejects robotic template feel; style and structure must coexist.
- Best fit: concise narrative cards with visible metrics + rationale, not rigid report headers alone.

### Blockers
- None.

### Active Tasks / WIP
- Apply DVMR-by-default style consistently in subsequent Telegram direct responses.

---

## Session Distill ("No response generated" incident)

### Decisions
- User requested immediate root-cause + mitigation.
- Operational decision: perform fast recovery first (gateway restart) after log-level cause confirmation.

### State Changes
- Cause identified from gateway log:
  - `telegram final reply failed: ... empty formatted text and empty plain fallback`
  - timestamp: `2026-03-06 10:07:30 KST`.
- Mitigation applied:
  - gateway restarted via `gateway.restart`.
  - health rechecked: Gateway OK / Telegram OK / Discord OK.

### Lessons
- `No response generated` can map to outbound payload-empty failure, not only upstream model timeouts.
- Fastest low-risk recovery for transient send-path corruption is gateway restart + immediate health verification.

### Blockers
- Root code-path generating empty final payload remains internal OpenClaw behavior (not fully eliminated).

### Active Tasks / WIP
- If same signature recurs (>=2 within 24h), add automated detector/alert rule for telegram empty-payload failure.

---

## Session Distill (skill-apply request + excluded-skill explanation)

### Decisions
- User requested immediate execution: "apply 5 skills first", and asked for explanation of excluded candidates (`pdf`, `transcribe`, `gh-fix-ci`, `openai-docs`).
- No explicit final answer sent yet because the exact "5 skills" set was not recoverable from current visible context.

### State Changes
- Performed context recovery attempts:
  - memory search and same-day memory review
  - session/transcript grep against prior assistant snippet
  - `openclaw skills check` run to validate currently eligible skills (123 eligible / 38 missing requirements)
- Verified available nearby skill signals:
  - built-in overlap candidates exist (`pdf-analyzer`, `summarize`, `openai-whisper`, `github`/`gh-issues`) matching user’s excluded list theme.

### Lessons
- Reply-context snippets can omit the actual earlier "top 5" list; quote-only context is insufficient for direct execution without reconstruction.
- For skill-apply requests, capture and persist the proposed skill list in-session to avoid lookup ambiguity on follow-up turns.

### Blockers
- Primary blocker: missing source list for the requested "5 skills" in currently retrievable context.
- Secondary blocker: high-noise transcript grep output slowed confirmation.

### Active Tasks / WIP
- Next response pending: provide concise mapping + confirm the exact 5 skills to apply, then execute immediately.
- Include clear explanation of excluded candidates (`pdf`, `transcribe`, `gh-fix-ci`, `openai-docs`) with overlap/duplication rationale.

---

## Session Distill (Recall adoption)

### Decisions
- Adopted recall-style memory workflow immediately (A안): daily structured capture + recall readiness check.
- Success criterion fixed: restore previous decisions/tasks within 30 seconds after session restart.

### State Changes
- Added script: `scripts/recall_quick_check.py`.
- Added runbook: `docs/ops/RECALL_ADOPTION.md`.
- Updated heartbeat checklist to run recall check daily.
- Initial check result: `restore_readiness_score=100`, `status=PASS`.

### Lessons
- Operational memory quality improves when daily files keep strict sections (`Decisions/Blockers/Active Tasks / WIP`).
- Fast readiness scoring makes regressions visible before context-loss incidents.

### Blockers
- None for baseline adoption.

### Active Tasks / WIP
- Keep daily execution of `memory_promote.py` + `recall_quick_check.py` and watch readiness drift.

---

## Quick Capture (user keep-note)
- User requested to save for later use: GitHub repo `https://github.com/vicnaum/bun-demincer` (Bun standalone binary decompiler/deobfuscator pipeline) for future reference/use.
