# D2-2 A-Plan 실행 보고서 (2026-03-08)

## 0) 완료 정의
D2-2 게이트를 동일 환경에서 2회 실행해 절대 기준+회귀 기준 동시 통과 여부를 확인하고, 둘 다 통과 시에만 지정 순서로 카나리 재활성화 수행.

## 1) Preconditions Snapshot

### 1-1. 후보 4개 잡 disabled 상태 확인 (cron list)
- 3f03a097-c90a-4c0a-a9d4-8afd28ea7734: `enabled=false`
- 5e7ba4c8-6f89-4dc6-b4cf-58ca31c5b54c: `enabled=false`
- 80a3884e-354e-4c7d-b3a4-03ca35cc5ce8: `enabled=false`
- 96ddcade-adca-46b1-bd7d-4f1a9558529a: `enabled=false`

근거: `/tmp/cron_list_all.json` (openclaw cron list --all --json 결과)

### 1-2. baseline / 이전 지표 파일 확인
- baseline: `.state/recall_router_baseline.json`
  - timestamp: `2026-03-07T17:32:20.805212`
  - baseline hybrid p95: `32.3026ms`
- 이전 metrics: `.state/recall_router_eval_metrics.json` 존재
- 관련 아티팩트:
  - `.state/recall_router.log.jsonl`
  - `data/recall_eval_samples.json`
  - `.github/workflows/recall-router-eval.yml`

### 1-3. Cooldown 30분 조건 검증
- 후보 4개 잡의 마지막 업데이트 시각으로부터 경과 시간(실행 시점 기준): 최소 `677.1분`
- 실행 중 재활성화 변경 없음
- 판단: **Cooldown(>=30분) 충족**

## 2) D2-2 게이트 실행 (동일 환경 2회)

워크플로우 기준 명령(`.github/workflows/recall-router-eval.yml`) 기반:

```bash
python3 scripts/recall_router_eval.py \
  --min-top3-hit-rate 90.0 \
  --max-hybrid-p95 40.0 \
  --max-error-count 0 \
  --min-route-match-rate 95.0 \
  --fail-on-regression \
  --save-baseline
```

운영 메모:
- 텍스트 리포트 모드에서 스크립트 내부 `KeyError: 'op'` 출력 버그가 있어,
  동일 게이트/회귀 동작을 유지하기 위해 `--json`으로 2회 실행함.
- 실행 산출물 보존:
  - `reports/.tmp/d2-2-run1.json`
  - `reports/.tmp/d2-2-run2.json`
  - `reports/.tmp/d2-2-run1-metrics.json`
  - `reports/.tmp/d2-2-run2-metrics.json`

## 3) Run1/Run2 Metrics

| Run | Exit code | gate.pass | top3_hit_rate | route_match_rate | route_p95.hybrid (ms) | error_count | latency_vs_baseline (increase %) |
|---|---:|---|---:|---:|---:|---:|---:|
| run1 | 2 | false | 100.0 | 100.0 | 37.3759 | 0 | +15.71% |
| run2 | 2 | false | 100.0 | 100.0 | 37.9536 | 0 | +17.49% |

해석:
- 절대 기준(quality/오류/절대 p95<=40ms)은 통과
- 회귀 기준(latency_vs_baseline <= +5%)은 **2회 모두 실패**

## 4) Pass/Fail 판정 및 근거
- 종합 판정: **FAIL (NO-GO)**
- 근거:
  1) run1, run2 모두 `--fail-on-regression` 기준으로 exit code `2`
  2) `latency_vs_baseline` 증가율이 각각 `+15.71%`, `+17.49%`로 허용치 `+5%` 초과
  3) A-plan 조건(2회 모두 절대+회귀 게이트 통과) 미충족

## 5) Canary Actions (enable/rollback)
A-plan 규칙상 게이트 2회 동시 통과 실패이므로 카나리 재활성화 **미실행**.

- a) 80a3884e... : 실행 안 함
- b) 5e7ba4c8... : 실행 안 함
- c) 96ddcade... : 실행 안 함
- d) 3f03a097... : 실행 안 함
- 롤백 발생: 없음 (활성화 자체 미실행)

## 6) 최종 권고
**keep disabled**

사유: 회귀 게이트 실패가 2회 연속 재현되어, 현재 시점 재활성화는 p95 회귀 리스크가 높음.

---

### 실행 증거 요약 파일
- `/tmp/cron_list_all.json`
- `reports/.tmp/d2-2-run1.json`
- `reports/.tmp/d2-2-run2.json`
- `reports/.tmp/d2-2-run1-metrics.json`
- `reports/.tmp/d2-2-run2-metrics.json`
- `.state/recall_router_baseline.json`
