# Script Attic

Archived on `2026-03-06`.

Purpose:

- keep clearly superseded entrypoints out of the top-level `scripts/` directory
- preserve source for reference until the replacement has operated cleanly for a while
- make inventory output distinguish `retired` from `orphaned`

Current moves:

- `auto_post.sh` -> replaced by `auto-blog-post.sh`
- `batch_posts.py` -> replaced by `auto-blog-post.sh`
- `monitor-brain-link-ci.sh` -> replaced by `github-ci-monitor.sh`
- `fetch_unsplash.py` -> archived as unused helper
- `memory-grep-search.sh` -> archived as unused helper
- `cron-daily-diff-report.py` -> archived after candidate sweep
- `notion_log.py` -> archived after candidate sweep
- `usdkrw_alert.py` -> archived after candidate sweep
