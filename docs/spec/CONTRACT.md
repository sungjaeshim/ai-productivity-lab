# SPEC Contract

- `SPEC.md` is the canonical orchestration specification.
- Keep Markdown body in English and Korean text only in HTML comments.
- Before shipping SPEC changes, run:
  `python3 scripts/validate_spec_contract.py`
